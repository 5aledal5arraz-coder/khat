# سجل فريق خط — Team Log

> تحدّثه مريم (mariam). دايمًا مختصر: جدول حالة + قرارات بسطر واحد. المهام المنجزة تنطوي بسطر.

## الحالة

| التاريخ | المهمة | المسؤول | الحالة |
|---|---|---|---|
| 2026-07-30 | **صفحة التسجيل اللايف `/admin/recording/[roomId]/v2`** — ٣ ملاحظات خالد، مُراجَعة (fahad تقنياً · sara تصميماً · yousef أمنياً/DB) و**كل الأسئلة مُجاوبة من خالد بنفس الجلسة → القرارات نهائية**. ٤ مراحل | omar | ⏳ قيد التنفيذ |
| 2026-07-30 | ↳ المرحلة ١+٢ كوحدة واحدة: إصلاح انزياح الـ pause + إعادة التصوير (`retake` + `room_takes` + `take_number`) | fahad | ⏳ قيد التنفيذ |
| 2026-07-30 | ↳ المرحلة ٣ (تصدير CSV+EDL @24fps) + المرحلة ٤ (تشك-ليست ١٧ بند) | fahad | ⏸ ما بدأت |
| 2026-07-30 | ↳ QA مستقل لصفحة التسجيل | noura | ⏸ ما بدأ |
| 2026-07-30 | 🔴 التحقق من سجل الـ migrations في **الإنتاج** (قراءة فقط بتصريح خالد) | yousef | ⏳ النتيجة لم تصل |
| 2026-07-30 | **صفحة المخرج + أثر الطاقة** (طلبان من خالد) — النطاق و١٦ معيار قبول مُسلَّمة، ٣ موجات، **٣ أسئلة موقوفة على خالد**. راشد يقيس أثر الفرز · سارة تصمّم التفاعل. **local فقط — لا نشر ولا migration** | mariam | ⏳ بانتظار قرارات خالد ثم «نفّذ» |
| 2026-07-29 | **إظهار التصنيفات في الموقع العام** — خالد وافق على الثلاث قطع: (١) شارة تصنيف على الكرت وصفحة الحلقة (٢) فلتر `?category=` في `/episodes` (٣) صفحات `/categories/[slug]`. معايير القبول مُسلَّمة (١٥ معيار) + ٥ قرارات موقوفة على خالد. **local فقط — لا نشر** | mariam | ⏳ بانتظار قرارات خالد ثم «نفّذ» |
| 2026-07-29 | ↳ المراجعة البصرية للشارة/الفلتر/صفحة التصنيف (RTL) | sara | ⏳ بالتوازي |
| 2026-07-27 | **خطة الإصلاح الشاملة** — مُسلَّمة: `.claude/studio-fix-plan-2026-07-27.md` (تحقق مستقل من 3 ادعاءات حرجة · جدول مطابقة كامل لكل ادعاء في تقرير الفحص · 28 بند إصلاح في 6 موجات بمعايير قبول من محاور مريم السبعة · تصحيح التكلفة بأرقام مقيسة · 9 قرارات لخالد). **صفر تعديل على كود المصدر** | omar | ✅ منجز — بانتظار «نفّذ» من خالد |
| 2026-07-27 | ↳ **تشغيل حي بميزانية $8 معتمدة** — حلقة `ZPeBeS87EeI` (3س36د)، الجلسة `09f31ded`: **48 نداء · $2.5996 · ~30 دقيقة**. أطول حلقة في القاعدة، الخط الثماني كامل | noura | ✅ منجز |
| 2026-07-27 | ↳ نموذج تكلفة بأسعار متحقق منها حياً + اكتشاف `buildTimedSegmentsFromVtt()` (مبنية ومختبَرة وصفر استدعاء إنتاج) + `prompt caching` يقلب ق-1 من كلفة إلى توفير | rashid | ✅ |
| 2026-07-27 | ↳ تثبيت بندَي الأمان + اكتشاف أن `speaker` لا يصل الموقع العام إطلاقاً (كل اقتباس يُنسب للضيف بالاسم والصورة) وأن حافة `finalized → published` مبنية وفاضية | yousef | ✅ |
| 2026-07-27 | **فحص الاستوديو الشامل** — التقرير النهائي مُسلَّم: `.claude/studio-audit-2026-07-27.md` (10 أقسام: خلاصة، شرح بلغة خالد، حكم المحاور السبعة، 11 عطلاً، خطة 6 موجات، 8 قرارات مطلوبة، ما لم يُفحص، ملحق تقني). **صفر تعديل على كود المصدر — القيد احتُرم بالكامل** | omar | ✅ منجز — تجاوزته خطة الإصلاح |
| 2026-07-27 | ↳ تحقق عمر المستقل من 5 ادعاءات متصدّرة + 4 إضافية: 7 صمدت، **2 لم تصمد** (توقيع الفصول/المقاطع ما فيه وسيط intelligence أصلاً → الجهد أكبر من تقدير راشد؛ «40 من 41 حلقة تنضغط» غلط → الصحيح **19 من 19 حلقة كاملة**، جدول episodes ثنائي القمة) | omar | ✅ |
| 2026-07-27 | ↳ المحاور 1–6 (كود · AI · QA · UX/RTL · أمن · توزيع) + تعريف «أعلى جودة» — كلها مُسلَّمة داخل التقرير | fahad/rashid/noura/sara/yousef/marzouq/mariam | ✅ (محور QA جزئي: 9 من 11 مخرج ما تولّدت) |
| 2026-07-18 | فحص اللوحة الشامل + المرحلة 1 (6 مهام) + ثغرة 1-5 + التيزر v1 — كلها أُغلقت رسميًا بعد QA نورة المستقلة | omar/fahad | ✅ منجزة |
| 2026-07-25 | إعادة تصميم `/admin/ops` — Wave 1 منشور على الإنتاج (`ba2d918`) | — | ⏳ Wave 2 ما بدأ |
| 2026-07-25 | `teaser_questions` — مواصفة مقفلة (عدّاد بالوارد + `/admin/teaser-questions`، بلا migration) | — | ⏸ ضمن Wave 2 |
| 2026-07-19 | قسم دعوة الضيف — خطة + 8 معايير قبول جاهزة | — | ⏸ بانتظار «نفّذ» من خالد |
| 2026-07-21 | تعريب taxonomy السوق + قياس الزخم (يحتاج snapshots زمنية) | — | ⏸ ما بدأت |
| 2026-07-18 | المرحلتان 2 و3 من خطة اللوحة | fahad | ⏸ ما بدأت |

## تعريف «أعلى جودة» للاستوديو (mariam، 2026-07-27 — مرجع الحكم النهائي)

سبعة محاور، كل محور له مقياس ومصدر رقم وخط أساس. القياس على **عيّنة 3 حلقات حقيقية** لا حلقة وحدة.

| # | المحور | المقياس | خط الأساس المقبول |
|---|---|---|---|
| ج1 | دقة النص المستخرج | `transcript-quality.ts`: maxConsecutiveRun · uniquenessRatio · إشارات intra-segment + مقارنة 3 نوافذ ×60ث مع الصوت | صفر verdict «degenerate» يعدّي · uniqueness ≥0.85 · صفر خطأ اسم عَلَم بالنوافذ |
| ج2 | أمانة المحتوى (لا هلوسة) | كل timestamp/اقتباس/اسم بالمخرجات يُطابق نصيًا على الـ transcript | **صفر تسامح**: 100% من الأرقام والاقتباسات مُثبتة، وإلا المخرج مرفوض |
| ج3 | جاهزية النشر | نسبة الحقول اللي عدّلها خالد قبل النشر (قياس مباشر على حلقة) | ≥70% من الحقول تُنشر كما هي · صفر حقل يحتاج إعادة توليد كاملة |
| ج4 | سلامة العربية | حكم خالد الأعمى على 10 مقتطفات (مو حكم موديل — أثبتنا 2026-07-26 إن حكم الموديل للعربي بلا قيمة) | صفر إنجليزي مُقحَم · صفر ترجمة حرفية · اللهجة مطابقة لصوت البودكاست |
| ج5 | القوة التسويقية | فحص قواعد (طول العنوان، هوك بأول 15ث، بنية الوصف، عدد المقاطع) + حكم مرزوق مقابل سلوك المنصات 2026 | 100% مطابقة القواعد · ≥3 مقاطع صالحة للنشر بلا قص يدوي |
| ج6 | سرعة الإنتاج | `ai_runs.latency` مجمّعة على `subject_id` + زمن الحائط من الرفع لجاهز-للنشر | ≤45 دقيقة لحلقة ساعتين · صفر مرحلة تعلّق بلا رسالة خطأ |
| ج7 | التكلفة/الحلقة | `SUM(cost_usd)` من `ai_runs` حيث `subject_table='studio_sessions'` — شامل Whisper | رقم واحد معلن ومُثبت لكل حلقة · صفر استدعاء AI خارج الراوتر (غير محسوب) |

## القرارات

- 2026-07-30 — ⚑ **توضيح خالد يقفل النطاق نهائياً**: «الأسئلة تم توليدها في **مرحلة الإعداد**، وأنا كمقدم **أوافق على الإعداد كامل بكامل أسئلته**، ففي التصوير اللايف **كل ما أحتاجه تغيير ترتيب الأسئلة الموجودة**». → الميزة كلها ثلاث قطع: (أ) إعادة ترتيب موجود (ب) مصافحة المخرج يقترح ← المقدم يوافق (ج) زر البدء + المؤقّت على شاشة المخرج. **التوليد الحيّ والبدائل المُولَّدة مسبقاً وأي حقل يستلزم إعادة توليد التحضيرات — مقفولة بقرار خالد نفسه لا بتوصية منّا** (يبقى مطروحاً: حقل **مُشتق** بلا توليد وبلا migration إن احتاجه راشد). **ثابت إلزامي: الترتيب فقط — صفر تغيير نص، صفر حذف، صفر إضافة.**
- 2026-07-30 — **قرارات خالد النهائية + إعادة ترتيب الموجات (mariam)**: **الاتجاه ينقلب** (طاقة هادئة ← سؤال **يرفع** التوتر، تصحيح لا مطابقة) · **ثلاث درجات صريحة** هادئ/متوسط/حادّ — **والمخزَّن يظل ٠–٥** (تغييره يغيّر معنى صفوف `energy_change` التاريخية) وتُشتق بـ`energyBand` · زر البدء **يُضاف** للمخرج بنفس القفل · **مؤشّر المقدم يبقى بيده** أثناء البث (خالف توصية سارة) · المؤقّت = الزمن الصافي نفسه + سطر لزمن القسم · الاقتراح يسقط بعد **٩٠ث** والقيمة المعروضة تتحدّث فوراً دائماً → **تمييز إلزامي: «القيمة المعروضة» حيّة للجميع، و«المعتمَدة للترتيب» لا تتحرّك إلا بفعل المقدم** · وبكاتبَين في الغرفة: **فعل المقدم يفوز فوراً ويلغي الاقتراح المعلّق**. **الترتيب الجديد**: م١ عدّاد المخرج · م٢ زر البدء · **م٣ ضبط دالة الفرز (تقدّمت)** · م٤ الاقتراح/الموافقة · ثم موجة الإعداد. سبب التقديم الأقوى: **نصفا الدماغ يتناقضان اليوم** — `coachHint` تصحيحية (`energy.ts:115`) و`scoreQuestionByEnergy` مطابِقة (`energy.ts:67-70`)، فالهمسة تقول ادفع والترتيب ينزّل سؤالاً هادئاً؛ **هذا وحده يفسّر «ما فيه تأثير»**. **صفر أسئلة معلّقة على خالد** عدا واحد: شكل المؤشّر (٣ أزرار أو ٥ نقاط مُسمّاة) — قرار سارة داخل حكم «المخزَّن ٠–٥».
- 2026-07-30 — ⚠️ **تصحيح نطاق صريح (خالد ينقض حكمي جزئياً — وهو صاحب القرار)**: «الطاقة في الإعداد» (وسم كل سؤال بدرجة قبل التصوير) صارت **داخل النطاق، موجة منفصلة** بعد ما كانت مرفوضة. **رفضي السابق كان مشروطاً بـ«إعادة توليد التحضيرات القائمة» لا بالحقل نفسه** — والحقل هنا يُكتب للتحضيرات **الجديدة** فقط، داخل `prep_v2` JSONB: صفر migration · صفر backfill · صفر نص يتغيّر. **رأس معايير الموجة ومانع للشحن: تحضير قديم بلا الحقل يسلك تماماً كما اليوم — ترتيب متطابق سطراً بسطر عند كل درجة.** **«محرّر منحنى التوتر» → موجة ٣ لا هذه**: خالد ما طلبه، وهو سطح تأليف جديد يفتح سؤال «هل موافقة خالد على الإعداد تشمل المنحنى؟»، وقيمته لا تُقاس قبل تحضير موسوم حقيقي. (لا أنازع في قيمته — أنازع في توقيته.)
- 2026-07-30 — **حدود نطاق «صفحة المخرج + الطاقة» (mariam)**: داخل النطاق = زر بدء للمخرج **بنفس قفل التشك-ليست** · عدّاد حيّ على شاشة المخرج (إعادة استخدام `CompactClock`) · إسناد «مَن بدأ» **كعلامة بـ`author_id` لا كعمود** · تحويل تغيير المخرج للطاقة إلى **اقتراح+موافقة** · ضبط فرز `energy.ts` بعد قياس راشد. **خارج النطاق ويُرفض**: «سجل قرارات الطاقة» (مغطّى أصلاً بعلامة `energy_change`) · إيقاف/إنهاء/تغيير مرحلة للمخرج · أي migration أو نشر · عرض ساعتين معاً · صوت للتنبيه.
- 2026-07-30 — 🔴 **حكم التعارض مع بوابة التشك-ليست المنشورة اليوم**: زر بدء المخرج **تعديل لا إضافة** (يمسّ `change_phase:"host"` في `permissions.ts` + توجيه `recording-room-shell.tsx`). أربع قواعد: (١) زر المخرج **يخضع لنفس القفل** — أي زر لا يقرأ `isRecordingUnlocked` ينقض قرار خالد أمس. (٢) «تجاوز وابدأ» و«أكمل بنفسي» **للمقدم وحده** (صُمّما لغياب المخرج، فوجود زر عنده لا يبطلهما). (٣) الإسناد علامة بـ`author_id`، و`anchor_at` يظل مصدر الصفر الوحيد. (٤) **أول ضغطة تفوز** — الثانية تُرفض بهدوء بلا تيك ثانٍ.
- 2026-07-30 — **حقيقتان تغيّران شكل الطلب** (قرأتهما بالكود): (١) المخرج **اليوم** يغيّر الطاقة مباشرة وبصمت (`DirectorEnergyControl` + `change_energy:"director"`) — فطلب خالد **تعديل مسار قائم** وليس ميزة جديدة، وهو **يقلّل** ما هو منشور. (٢) `must_ask` **يسبق الطاقة** في مقارن `energy.ts` — فأي قسم كل أسئلته `must_ask` أثر الطاقة فيه **صفر رياضياً**؛ إن أثبته قياس راشد فالحل **ضبط معامل لا بناء**، ويُقدَّم لأنه أرخص وأسرع إحساساً.
- 2026-07-30 — **معيار إغلاق شكوى «الطاقة بلا تأثير»** — المقياس هو **حركة الترتيب** لا غير: خط الأساس X الحالي يُقاس ويُكتب أولاً (راشد)، ثم النقل ٥→١ **يبدّل السؤال المعروض التالي في ≥X٪ من الأقسام** (٣ أسئلة فأكثر) — الهدف المقترح ٧٠٪ ويُثبَّت بعد وصول خط الأساس. **إن طلع الأساس صفراً أو قريباً منه فهذا تفسير الشكوى نفسها.** + **حكم خالد بعد أول تصوير** — الشكوى كانت إحساساً فلا تُغلق بغير إحساس مؤكَّد برقم. ومعه **ثابت يُفحص مع كل معيار**: مجموعة معرّفات الأسئلة ونصوصها متطابقة حرفياً قبل وبعد، والفرق الوحيد المسموح هو الترتيب.
- 2026-07-30 — **إعادة التصوير (قرار خالد نهائي)**: نفس الحلقة ترجع `ready_to_record` كانتقال `retake` — خالد **وافق صراحةً على فتح رجوع للخلف** في عقد `lib/eir/transitions.ts` المُجمّد بقصد. + جدول `room_takes` (`anchor_at` تُكتب مرة واحدة · `camera_offset_ms`) + `room_session_markers.take_number` + إعادة تسمية `recording_ms` → `net_recording_ms`.
- 2026-07-30 — **التصدير**: CSV + EDL على **24 fps**. خالد اختار الاحتفاظ بـ EDL بعد ما عُرض عليه الدليل إن Resolve يُسقط العربي منها **بصمت**؛ الحل المتّفق عليه: EDL تحمل الموضع والنوع الإنجليزي واللون، والـ CSV جانبها تحمل الملاحظات العربية = **صفر فقدان**.
- 2026-07-30 — **تشك-ليست المخرج**: ١٧ بند / ٦ مجموعات، تتفتّح **بالتدريج بالمجموعة** (مو بند-بند)، مع «غير منطبق بسبب». زر «ابدأ التسجيل» **يُقفل فعلياً** — خالد رجّح القفل على توصية سارة الأخف، بس مع **مخرجَي أمان وافق عليهما**: «أكمل التشك-ليست بنفسي» + «تجاوز وابدأ» موثّق بسبب. «Black shade» = فلتر **Black Mist** على العدسة → صار عنصراً لكل كاميرا مو بنداً عاماً.
- 2026-07-30 — **ثلاث مشاكل قائمة أكبر من الملاحظات نفسها**: (١) **انزياح الـ pause** — الكاميرا تستمر تسجّل وقت الإيقاف المؤقت بينما `recording_ms` يستبعده، فكل علامة بعد أول استراحة منزاحة والانزياح **يتراكم**؛ لا فقدان بيانات (خالد أكّد إن كل تصوير سابق كان تجارب). (٢) **اختلاط التيكات** — ما كان فيه أي عمود يفصل تيكات إعادة التصوير. (٣) **المخرج ما يشوف شاشة «قبل البدء» أصلاً** — `recording-room-shell.tsx` يوجّه `host` فقط لكوكبيت المقدم.
- 2026-07-30 — ⚠️ **شرط تشغيلي على خالد**: حساب المخرج لازم يكون **`ADMIN` بالضبط** — لا EDITOR ولا OWNER. وخالد نفسه OWNER، فما يقدر يكون المخرج في نفس الغرفة.
- 2026-07-30 — **تصحيح حالة قاعدة البيانات المحلية (يُلغي drift ٢٥ يوليو)**: تحقيق يوسف أثبت إن الـ drift المحلي انصلح **نفس الليلة** في commit `ba2d918`؛ الـ ledger المحلي الآن **١٩/١٩ نظيف** و`db:generate` آمن (متحقّق بـ dry-run). شرطان قبل أي migration: **backup بـ `pg_dump` نسخة @16** + **مراجعة الـ SQL المولّد بالعين**. `db:push` و`db:adopt` **ممنوعان**.
- 2026-07-30 — ✅ **نشرة ثالثة بأمر خالد**: مؤشّر الطاقة (`e78d280`، **صفر migration**، ledger يظل ٢٣). شكوى خالد «ما يعطي تأثير» كان لها **ثلاثة أسباب متراكبة**: (١) `scoreQuestionByEnergy` بلا فرع `medium` والافتراضي ٣ ⇒ كل سؤال صفر ⇒ الترتيب no-op؛ (٢) `heroId` يثبّت السؤال بلا مصفّر ⇒ أول ضغطة من المقدم تقتل أثر المؤشّر للأبد؛ (٣) الاتجاه **مطابِق** بينما `coachHint` **تصحيحية** ⇒ الشاشة تناقض نفسها. + المخرج كان يعيد ترتيب قائمة المقدم **بصمت أثناء إجابة** ⇒ صارت اقتراحاً ينتظر الموافقة (القيمة فورية، الترتيب لا). + **اكتشاف**: `startTimer/pause/resume/end/setCurrentSection` **ما تبثّ إطلاقاً** ⇒ شاشة المخرج كانت متأخرة دائماً ومحد يدري.
- 2026-07-30 — **قرار خالد التحريري يُلغي الطاقة الصريحة**: «لا أحتاج في كل قسم خيار حاد». راشد قاس على أول تحضير حقيقي (`2fdb6a84…`، ٢٨ سؤالاً، $0.8105): **٤ أقسام من ٦ بلا أي سؤال حادّ**، وأثبت أن وسماً صريحاً **بدقّة ١٠٠٪** يساوي الدالة الحالية بالضبط — لا إشارة تُظهر شدّة غير موجودة في البنك. ⇒ أُلغي الحقل الصريح **وتعديل برومبت Pass 3**، وحلّ محلهما **سطر صدق** يُشتق من البيانات: «أسئلة هذا القسم متقاربة الشدّة». الصمت في تلك الأقسام كان **كل** الشكوى.
- 2026-07-30 — 🔴 **درسان من جولة الطاقة**: (١) **قرار تصميم صحيح أنتج الخلل** — فصل «المعروضة» عن «المعتمَدة» كان لازماً، لكن أحداً لم يسأل «ماذا لو انفصلا ثم لمس المقدم القرص؟» فحارس النقر قارن بالخطأ منهما و**مات الزر في قمرة قيادة حيّة**. ٢٠٥٢ اختباراً ما مسكته لأنها كلها تختبر المنطق، والخلل في **الجسر بين المنطق واليد** — ما كشفه إلا إصبع حقيقي. (٢) **قراءة-ثم-كتابة ليست ذرّية**: ضغطتان متزامنتان «تفوزان معاً» ٤ من ٥ حتى انتقل الحسم للقاعدة (`UPDATE … WHERE status <> 'live' … RETURNING`).
- 2026-07-30 — 🔧 **حقيقة أدوات عطّلت التحقق طول اليوم**: إحداثيات النقر في هذي البيئة بمقياس **×٣.٠٨ أفقياً و×٣.٠٦ عمودياً** (نقرة `[100,100]` تصل CSS `(308,306)`)، و`read_page` يرجّع شجرة فارغة و`innerText=0` فتبدو الصفحة ميتة وهي سليمة. **عايِر بمستمع DOM أولاً** ثم الواجهة كلها قابلة للنقر. وكيلان استنتجا خطأً أن «النقر لا يعمل».
- 2026-07-30 — ✅ **نشرة ثانية بأمر خالد**: اسم العضو و«الصفحة» (`28c8b5a`، ledger **22 → 23**). `admin_users.display_name` + `job_title` (nullable)، كتالوج سبع صفحات في الكود، و`resolveRoomRole()` يرجّح الصفحة على الاشتقاق من مستوى الصلاحية. **يُلغي قيد «حساب المخرج لازم ADMIN بالضبط»** — خالد يصير مالكاً ومقدماً في نفس الوقت. مُتحقَّق حياً: نفس حساب ADMIN، `job_title='host'` → كوكبيت المضيف، و`null` → شاشة المخرج. **باقٍ على خالد**: تعبئة الأسماء والصفحات الستة من `/admin/team` — قبلها الـCSV يقول «عضو الفريق».
- 2026-07-30 — 🔴 **درسان من نشرة الصفحات، يُقرآن قبل أي ميزة صلاحيات**: (١) `requireRoomRole` في `lib/collaboration/permissions.ts` **حارس تفويض حقيقي** كان يقرأ `room_participants.role`، وهذا العمود صار إسقاطاً لـ`job_title` — فحقل وصفي منح `delete_marker` و`end_room` وتغيير المرحلة على خمس نقاط. صار يشتق من `admin_users.role`، و`room_participants.role` للعرض فقط. (٢) **الاختبار اللي «يثبت» أن الصفحة ما تمنح صلاحية كان فراغاً** — يمرّن `requireActionRole` وهي لا تقرأ `job_title` فما كان يمكن يفشل. القاعدة: أي تأكيد «X لا يمنح صلاحية» لازم يمرّن الحارس اللي **يستهلك** X، لا حارساً يتجاهله. الاختبار الجديد مُثبَت بإرجاع الإصلاح (٦ من ١١ فشلوا).
- 2026-07-30 — 🔴 **صنف خلل عدّى كل البوابات مرتين في نفس اليوم**: مقارنة نوع SQL خاطئة (`uuid = text`) جعلت التصدير يرجع **500**، وعدّت من `tsc` ومن `next build` ومن ٢٠١٨ اختبار **لأن ولا واحد منهم ينفّذ SQL**. لم يُكتشف إلا بتنزيل HTTP حقيقي. الـsmoke صار ينفّذ استعلام التصدير الفعلي. **أي استعلام يُعدَّل لازم يُمرَّن على Postgres حقيقي.**
- 2026-07-30 — ✅ **نُشر على الإنتاج بأمر خالد الصريح («انشر»)**. Migrations **0019–0021** مطبّقة، الـ ledger **19 → 22/22 مطابق للكود**، و`room_takes` (٧ أعمدة) و`recording_checklist_items` (٩ أعمدة) وإعادة التسمية `recording_ms → net_recording_ms` كلها حيّة. `post-schema.sql` أُعيد تطبيقه. العمليتان `online` وكل المسارات العامة ٢٠٠. نسخة احتياطية للإنتاج قبل أي شي: `/root/db-backups/khat-prod-20260730-073504.dump`.
- 2026-07-30 — 🔧 **عقبتا نشر مثبَّتتان — تُقرأ قبل أي نشر قادم فيه migration**: (١) `npm run build` **ينفد من ذاكرة Node عند خطوة TypeScript** على الدروبلت (~٩٨٠MB، الحد الافتراضي) رغم وجود ٤GB swap — الـswap ما ينفع، السقف داخلي. الحل: `export NODE_OPTIONS=--max-old-space-size=3584`. (٢) `drizzle-kit migrate` **يتوقف للأبد على `[⣷] applying migrations...` عبر SSH غير التفاعلي** — بلا خطأ، بلا صف ledger، exit 1، والقاعدة ما تُلمس. ليست مشكلة SSL ولا env (الإعداد يستبعد `sslmode` و`psql` يتصل من نفس السيرفر)، و`setsid nohup` ما ينفع. الحل: **`scripts/migrate-node.ts`** (مهاجر drizzle-orm الداخلي — يكتب الـ ledger بنفسه فما فيه أي كتابة يدوية، وهي اللي عطبت التاريخ سابقاً). يُشغَّل بـ `KHAT_DB_TARGET=production npx tsx scripts/migrate-node.ts`. **ممنوع إدخال صفوف ledger باليد.**
- 2026-07-30 — **ترتيب النشر إلزامي بسبب إعادة التسمية**: رفع → بناء (بطيء) → migration → إعادة تشغيل **فوراً**. نافذة رؤية الكود القديم لعمود مُعاد التسمية تصير ثوانٍ. وأخطاء `Failed to find Server Action "…"` بعد إعادة التشغيل **طبيعية وعابرة** (تابات قديمة تحمل معرّفات قديمة)، ليست خللاً.
- 2026-07-30 — ✅ **أُغلق**: خالد سوّى فحص إغلاق تاب المخرج بنفسه على الإنتاج وأكّد أنه يشتغل، وعبّى الأسماء والصفحات الستة من `/admin/team` — فالـCSV صار يحمل أسماء عربية حقيقية بدل «عضو الفريق». **ما بقي أي بند معلّق من ملاحظات صفحة التسجيل.** (الملاحظة الأصلية للتاريخ:)
- 2026-07-30 — ~~🔴 **الفحص الوحيد المتبقي**~~: مسار الحضور عند **إغلاق تاب المخرج** لم يُجرَّب في متصفح حقيقي أبداً (نورة انقطعت ٥ مرات بخطأ خدمة 529). الخطأ الأصلي: `navigator.sendBeacon` **دائماً POST**، وPOST على مسار الانضمام = انضمام، فالمخرج اللي يقفل تابه يعلّم نفسه حاضراً ويحبس المقدم للأبد. الإصلاح (DELETE بـ`keepalive` + نبضة أولى فورية + تثبيت اللاتش بعد نجاح الـawait) مؤكَّد على مستوى الكود فقط. **الفحص: افتح بحساب المخرج، سكّر التاب، تأكد أن شاشة المقدم تنتقل لـ«ما فيه مخرج متصل».**
- 2026-07-30 — **شرط تشغيلي على خالد**: حساب المخرج لازم `ADMIN` **بالضبط** (`OWNER→host`, `ADMIN→director`, `EDITOR→editor`) — EDITOR ما يشوف القائمة، وOWNER يصير مقدّماً. وخالد OWNER فما يقدر يكون المخرج في نفس الغرفة.
- 2026-07-30 — ✅ **لغز «٦٦ → ٤١ حلقة» انحلّ**: خالد سوّاه بنفسه في جلسة منفصلة **بقصد**، ليجعل local مطابقاً لـlive بالضبط. ليس حادث فقدان بيانات ولا خلل أداة. **فأي فرق مستقبلي بين local وprod صار إشارة حقيقية لا ضجّة.**
- 2026-07-29 — **حدود نطاق «التصنيفات في الموقع العام» (mariam)**: داخل النطاق = `components/episodes/episode-poster-card.tsx` · `app/episodes/page.tsx` · `app/episodes/[slug]/page.tsx` · مسار جديد `app/categories/**` · تمرير اسم/سلاق التصنيف في مسار القوائم بـ`lib/queries/episodes.ts` (بلا N+1) · `app/sitemap.ts` إذا وافق خالد. **خارج النطاق ويُرفض**: تعديل نظام التصنيفات نفسه أو جدول `episode_categories` · دمجه مع `/topics` · تصنيفات متعددة للحلقة · إعادة تصميم `/episodes` · وصف/صورة للتصنيف (يحتاج migration) · أي تغيير في لوحة التحكم · أي نشر أو migration.
- 2026-07-29 — **حقائق مثبَّتة قبل البدء**: `episode_categories` فيه ٣ صفوف بسلاقات **عربية** (`سالفة` ١٦ · `مقاطع-خط` ٦ · `الموسم-الاول` ١٩)، ٤١/٤١ حلقة مصنّفة، صفر يتيم · `getEpisodes({category})` + `filterByCategory()` **موجودان وشغّالان** فالفلتر ما يحتاج طبقة بيانات جديدة · مسار القوائم يعبّي `category_id` فقط ولا يعبّي كائن `category` (الاسم/السلاق) — يُعبَّأ فقط في مسار التفصيل · `EpisodePosterCard` **مشترك** بين الرئيسية و`/episodes` و`/guests/[slug]` و`/topics/[slug]` والتوصيات، فأي شارة فيه تظهر في الخمسة · سابقة `decodeURIComponent(slug)` + `notFound()` داخل `generateMetadata` موجودة في `app/guests/[slug]/page.tsx` (تمنع soft-404).
- 2026-07-27 — 🚨 **حاجز نشر مثبَّت بالدليل**: 5 من 7 اقتباسات نُسبت للضيف «فيصل المحيني» **غير موجودة في الحلقة** (تطابق 25-43%، تحقق عمر المستقل). و`push-to-episode.ts:189` يكتب `status:"published"` ثابتاً، و`speaker` **لا يصل الموقع العام** فكل اقتباس يُنسب للضيف بالاسم والصورة مع زر يولّد صورة مشاركة. **ممنوع نشر أي اقتباس مولّد من الاستوديو قبل مطابقته بشرياً على النص.** الإصلاح الفوري سطر واحد (`"published"` → `"draft"` — الـRPC والقراءة العامة يدعمان `draft` أصلاً).
- 2026-07-27 — **تصحيحات على تقرير الفحص (انتفت أو صُحّحت)**: انحراف التوقيتات +52/+69 → **الأقصى الحقيقي +38.3د والوسيط +20.4د** · «الوسيط 68 حرفاً للعناوين» → **الوسيط 46 و6/6 تحت حد الجوال، العناوين سليمة** · «محرّر المقاطع يرمي المقاطع» → **لا أحد يرفضها، تمر صامتة** · **ضياع SSE ما ظهر (30/30)** حقيقي بالكود ولا ينطلق على localhost · **`maxDuration=300` ما قطع** (الخط 522ث؛ `next dev`/PM2 لا ينفّذانه) · «الاقتباسات التاريخية 8/8» → **0/8 مفبركة لكن 6 حرفية و2 إعادة صياغة** · «التوقيتات مجانية» → **على مسار الكابشنز فقط؛ Whisper يحتاج +$1.2954**.
- 2026-07-27 — **التكلفة المقيسة تُلغي التقدير**: **$2.5996 لحلقة 216 دقيقة · 48 نداء** (منها الخط الثماني $1.3042 وWhisper $1.2954 و13 نداء ضغط $0.3851). التقدير الأول (~$2.00 لحلقة ساعتين) كان **أعلى بـ39% للدقيقة وأقل بالنصف في عدد النداءات**. الحلقة بعد الخطة ≈ **$1.07 (−59%)**. نسبة العربي المقيسة **3.08 حرف/توكن** — لا تُستخدم قاعدة «4 أحرف/توكن».
- 2026-07-27 — **ق-1 و ق-5 و ق-6 تغيّروا بعد التشغيل الحي**: ق-1 (النص الكامل) من **+$0.58 كلفة** إلى **−$0.23 توفير** مع `prompt caching` (توصية: البديل الآمن `B′` أولاً، −$0.12 بلا إعادة ترتيب برومبتات) · **ق-5 انقلب**: Whisper ما اشترى دقة أسماء (الكابشنز أفضل في 11 اسماً مقابل 3) ورمى التوقيتات المجانية — البوابة تُرفع **لتفضّل الكابشنز** وWhisper احتياط فقط · **ق-6 ما عاد قراراً، صار حاجز نشر ينفَّذ فوراً**. ق-2/3/4/7/8 بلا تغيير جوهري.
- 2026-07-27 — **قرار جديد ق-9 بانتظار خالد**: قياس أثر `reasoningEffort` على الفصول والمقاطع = 4 نداءات ≈ **$0.10 صرف مدفوع**. راشد ما يشغّله بلا موافقة.
- 2026-07-27 — **ترتيب التنفيذ المعتمد**: موجة 1 (بوابة الاقتباسات → هوية الربط → الحذف المدمّر → عنوان الحلقة) · موجة 2 صفر تكلفة وتوفير صافٍ (الكابشنز افتراضاً → التوقيتات من الـVTT · `max_tokens` · حارس الكاش **متلازم مع** زر إعادة التوليد · بتر النص والجزء السادس) · موجة 3 النص الكامل (**يجب أن تسبق أي حكم على الجودة**) · موجة 4 صدق الحالة · موجة 5 النمو · موجة 6 الهيكلة والقياس.
- 2026-07-27 — ⚠️ **النسخة الاحتياطية ليست ضمانة**: `lib/episodes/versions.ts:33-36` يبتلع فشل الـFK ويرجّع stub، وبما أن `episodes.id` = معرّف يوتيوب وكثير من الحلقات بلا صف في الجدول، `saveVersion` **ينسكب بصمت في الحالات نفسها التي نريد حمايتها**. لا يُعتمد عليها قبل إصلاحها (ص-27).
- 2026-07-27 — **قيد مهمة الاستوديو**: ممنوع تعديل كود المصدر قبل تسليم التقرير كامل لخالد (أمر صريح منه). local فقط، production مجمّد لهذي المهمة. التوصيات تنكتب ولا تُنفَّذ. **الحالة: التقرير سُلِّم؛ القيد باقٍ لين خالد يقول «نفّذ».**
- 2026-07-27 — **8 قرارات موقوفة على خالد** قبل أي تنفيذ (تفاصيلها في التقرير §6): ق-1 تمرير النص الكامل (+$0.58/حلقة، بديل أرخص +$0.20) · ق-2 اللهجة الكويتية للخطّاف · ق-3 المقاطع 10-20 موزّعة → 8 مرتّبة · ق-4 طول المقطع حتى 180 ثانية · ق-5 رفع بوابة جودة النص (+$0.72 على الحلقات الرديئة) · ق-6 الاقتباسات مسودة بدل نشر مباشر · ق-7 مصير المولّدات اللي تطلب المستحيل · ق-8 الهيكلة 12→7.
- 2026-07-27 — **موجة 1 لا تحتاج أي قرار** (الحذف المدمّر، زر إعادة التوليد، السقوف المخنوقة، كذب الحالة، عنوان الحلقة، الحاكم، البث، جسر المقاطع). يوسف يعتبر بندين منها **مانعين للنشر**. ⚠️ لا يُختبر إصلاح الحذف على قاعدة فيها شغل حقيقي بلا نسخة احتياطية.
- 2026-07-27 — **تحذيران تشغيليان لخالد من اليوم** (بلا كود): لا يُحذف أي studio session لحلقة منشورة · زر «إعادة التوليد» لا يعمل، فالحل الوحيد جلسة جديدة.
- 2026-07-27 — **حد المصداقية**: خط إنتاج الاستوديو ما اشتغل كامل ولا مرة محلياً. الحكم على الجودة مبني على **مخرج واحد (الفصول) من حلقة 8:24**. أي حكم أوسع يحتاج تشغيل حلقة صوتية كاملة أولاً.
- 2026-07-27 — **حدود نطاق فحص الاستوديو (mariam)**: داخل النطاق = `app/admin/studio/**` · `lib/studio/**` · مولّدات الاستوديو في `lib/ai/` · مسارات إدخال النص (yt-dlp captions / youtube-audio / Whisper) · حتى حدود `push-to-episode.ts`. **خارج النطاق ويُرفض**: discovery / season planning / الموقع العام / `/admin/ops` / market intelligence / هوية الضيف / أي migration أو تنظيف بيانات أو نشر.
- 2026-07-27 — **حكم العربية بشري لا آلي**: بناءً على بنشمارك 2026-07-26 (حكم الموديل طابق خالد 35% مقابل 34% صدفة)، أي حكم على جودة العربية بهذا الفحص يجي من خالد أو مرزوق — ممنوع الاستشهاد بحكم موديل كدليل جودة لغوية.
- 2026-07-25 — **تفويض خالد**: الفريق يحسم القرارات المهنية بنفسه بلا أسئلة ارتجاعية، والسقف = أفضل تجربة لا الحد الأدنى. (لا يشمل: الحذف/النشر/الإنتاج — تظل بموافقة صريحة.)
- 2026-07-18 — أي حذف / تصفير / ترحيل / كتابة فوق بيانات — محلي أو إنتاج — يحتاج موافقة صريحة من خالد.
- 2026-07-18 — التواصل مع خالد بالعربي الكويتي الواضح؛ الكود والمصطلحات التقنية تظل بالإنجليزي.
- 2026-07-18 — راشد يملك بنود طبقة الـ AI (تجاوزات getClient خارج الراوتر، صدق تليمتري `ai_runs`، قرارات الموديلات عبر benchmarks، مراجعة البرومبتات) — والتنفيذ يظل عند فهد وحده.
- 2026-07-21 — كل محتوى «التفكير الأصيل» و«إشارات السوق» عربي بالكامل؛ عناوين المصادر الخام الخارجية (YouTube/Apple) تبقى كما هي.
- 2026-07-21 — **تنبيه بنيوي قائم**: `persistSignal` (`lib/market-intelligence/ingestion.ts`) يسوي `ON CONFLICT DO UPDATE` يطمس `view_signal` → ما فيه سلسلة زمنية، وقياس الزخم مستحيل بلا snapshots. نفس القيد يمنع أي «سهم اتجاه» بأي صفحة.
- 2026-07-25 — **رُفض بمبرّر (منع تضخّم `/admin/ops`)**: ملخّص AI للرئيسية · أزرار قبول/رفض من صفحة قراءة · أسهم اتجاه. **قُبل**: تحديث كل 60ث يتوقف لما التبويب مخفي · نزول بطاقات النبض تحت الـ launchpad.
- 2026-07-25 — **مدى `teaser_questions` مقفل**: شريحة بالوارد (`status='pending'`) + صفحة `/admin/teaser-questions` بثلاثة إجراءات عبر `requireActionRole('EDITOR')` + (تابع اختياري) نموذج عام. **صفر migration وصفر عمود جديد**؛ أي حقل تدقيق (`reviewed_by`/`reviewed_at`) خارج المدى. القرّاء يرجّعون أعمدة عامة فقط — `ip_hash`/`user_agent` ما تغادر السيرفر.
- 2026-07-25 — **تصحيح واقعي مثبّت**: ما فيه نموذج عام لأسئلة الجمهور إطلاقًا؛ `POST /api/teaser/[id]/questions` ما يناديه أي كود. `teasers` = 0 صف · `teaser_questions` = 0 صف · وكل قنوات الوارد البشرية = 0 صف.
- 2026-07-19 — **قسم دعوة الضيف — قرارات خالد** (تنتظر «نفّذ»): يجهّز الرسالة ويرسلها بنفسه (صفر إرسال مؤتمت) · وافق/رفض يدوي عبر `status.ts` · تمديد `guest_candidates` لا قسم مكرّر · التلفون/الإيميل ينتقلان لجدول `guests` عند الترقية (إداري غير عام) → يحتاج عمودين + migration.
- 2026-07-19 — **بندان مُتتبَّعان لا يحجبان**: ترحيل 137 موضع server actions لـ `requireActionRole('EDITOR')` · `npm run lint` يفشل على 8 أخطاء `no-explicit-any` قديمة بملفّين خارج شغل فهد.
- 2026-07-19 — **مقترح مقبول**: صفحات 404 الأدمنية ترجع HTTP 200 (قيد streaming بالـ App Router) — وظيفيًا سليمة، نقبل الـ200.
