# سجل فريق خط — Team Log

> تحدّثه مريم (mariam). دايمًا مختصر: جدول حالة + قرارات بسطر واحد. المهام المنجزة تنطوي بسطر.

## الحالة

| التاريخ | المهمة | المسؤول | الحالة |
|---|---|---|---|
| 2026-08-12 | ⛔ **فكرة «خط مقروء» / «المكتبة» — مرفوضة من خالد، لا تُقترح ثانيةً.** الفريق (مرزوق نمواً، مريم منتجاً) اقترح تحويل الأرشيف إلى نص عربي مهيكل قابل للبحث والفهرسة. **خالد رفضها صراحةً 2026-08-12 وطلب مسحها.** وكذلك أُلغي بند التحليلات بطلبه. ✅ **ما يبقى صالحاً من الجلسة** (حقائق مقيسة لا مقترحات): الموقع العام بلا أي تحليلات (`app/layout.tsx` صفر `<Script>`) و`analytics_events` صفر صف وصفر كاتب · `episodes.view_count` مشاهدات يوتيوب لا زيارات · `buildTimedSegmentsFromVtt` مبنية ومختبَرة وبلا مستهلك · و**عطل كامن**: `lib/seo/episode-jsonld.ts:142` يبثّ `FAQPage` بجواب ثابت مزيّف — لا يظهر اليوم لأن `deepAnalysis` فارغ، وينفجر أول تشغيل لـStudio | — | ⛔ مرفوضة |
| 2026-08-12 | ⛔ **م-٤ أُغلق — التدقيق كان غلطاً، ولا تغيير**. ادّعيت أن جدولَي `timestamps`/`resources` و`episodes.summary` «بلا كاتب **ولا قارئ**». النصف الثاني خطأ: الثلاثة تُقرأ من `fetchDbEpisodeDetail` (مسار الاحتياط عند فشل يوتيوب)، وبترتيب **مقصود وموثّق**: الإثراء يغلب والجدول/العمود القديم يسند. ✅ ما تأكد: **لا كاتب لأيٍّ منها في المشروع كله** ⇒ طبقة خاملة، لكنها مرتّبة صح. **قرار: لا حذف** — إسقاط جداول من الإنتاج غير قابل للتراجع ويحتاج إذناً صريحاً ومكسبه صفر، والأسوأ أنه يزيل احتياطاً موثّقاً لسبب تجميلي. ما كلّف وقتاً أمس هو ترتيب القراءة غير المكتوب، وقد عولج في م-٥. **باقٍ منه لخالد**: مراسي `sec-*` — أهداف روابط عميقة صالحة، والغائب الحقيقي فهرس محتوى للصفحة = قرار منتج لا تنظيف | خالد | ⏳ قرار |
| 2026-08-12 | ✅ **م-٥ منشورة** — صفّان يغذّيان «ملخص الحلقة» (`enrichment.full_summary` و`episode_overrides.custom_description`) واللوحة تعطي زرّاً واحداً يكتب الثاني. **إصلاح أمس كان نصف إصلاح**: كان يشتق `summary` فقط حين يصادف أنه نسخة من الوصف — فمع أول دفعة Studio كان تعديل «الوصف» سيصمت من جديد. القاعدة الآن منطوقة: **تعديل المحرّر يغلب النص المولَّد دائماً**. وبما أن ذلك يجعل دفعة الملخص وحدها بلا أثر، صارت الدفعة تحمل الـoverride معها — **وهذا أعاد فتح ثغرة ص-٤ من الجهة الأخرى**، فوُسِّع حارس `needsTitle` ببند رابع. الاتجاهان مفحوصان بالطفرة. commit `9189b25` · ٢٩٣٤ اختباراً · اللايف: كتلة الملخص ١٠٩٧ حرفاً بلا هاشتاقات | — | ✅ |
| 2026-08-12 | ✅ **م-٧ منشورة** — تبويب «إثراء الحلقة» صار يعرض **الحقول الخمسة التي لا يملكها**: `hero_summary` · `full_summary` · `takeaways` · `resources` · `timestamps` — بقيمتها الحالية، وبما تتحكم فيه على الصفحة العامة، وعدّاد ممتلئ/الكل، ورابط للاستوديو. **للقراءة فقط عمداً**: مسار كتابة ثانٍ لنفس الصفوف هو ما أنتج مصدرين لـ`full_summary` (م-٥). الحلقة بلا EIR تعرض السبب بدل رابط ميت. الحدّ لم يُنقل — أصبح مرئياً. commit `f2de630` | — | ✅ |
| 2026-08-12 | ✅ **م-١٤ منشورة** — حُذف «اكتشف أكثر» و«الحلقة السابقة/التالية» من صفحة الحلقة بقرار خالد. ومعهما ماتت البنية التحتية كاملة: `episode-connections.tsx` · ٤ خصائص · **٣ استعلامات لكل مشاهدة** (الحلقات المجاورة + اقتباسات الرئيسية + التأملات) · `getCachedAdjacentEpisodes` · `selectAdjacentEpisodes` واختباراتها الأربعة · و`getAdjacentEpisodes` التي ظهرت أثناء التنظيف و**لم يكن لها مستهلك أصلاً**. على اللايف: الصفحة تنتهي بـ«حلقات لها علاقة» (٣ بطاقات)، والراعي سليم. commit `db050dd` · ٢٩٣٠ اختباراً | — | ✅ |
| 2026-08-12 | ✅ **م-١٥ منشورة** — بلاطة برتقالية لأيقونة اللوحة (`app/admin/icon.svg` + `apple-icon.png`) من نفس مولّد الأيقونات. **الكلفة مقيسة**: العاجي على البرتقالي **3.03:1** مقابل 10.78:1 — لذلك الاستثناء محصور بـ`app/admin/` وكل سطح عام يبقى نيلياً. تحقّق: `/` تُصدر `/icon.svg`، `/admin/*` تُصدر `/admin/icon.svg`، والبايتات على اللايف مطابقة للمحلي (md5 `664fd400…`) ومختلفة عن أيقونة الموقع. **حدّ مذكور**: `favicon.ico` جذري فقط في Next — لا تجاوز لكل مقطع — فالـ.ico يبقى نيلياً في المتصفحات التي تفضّله | — | ✅ |
| 2026-08-12 | ✅ **عدد الضيوف صار تحكّماً** — إعداد `thinkers_limit` (١–٤٠، بلا ترحيل) يقود المخزون والخانات والشريحة معاً بدل ثلاثة أرقام مزروعة (12 / 3 / لا شيء). المخزون على الإنتاج صار **١٩** بدل ١٢. حارسان مسكاني: سلّم الخط رفض `rounded` المجرّدة و`text-sm`، و**البناء** رفض استيراد الثوابت من ملف يستورد `lib/db` داخل مكوّن عميل (tsc لا يراها) — نُقلت لـ`lib/homepage/hall.ts` | — | ✅ |
| 2026-08-11 | ✅ **م-١١ منشورة ومتحقَّقة** — شريط الضيوف فوق الشبكة (صور دائرية + اسم + تمرير)، الشبكة حلقات فقط، و«قريباً» بإطار متقطّع وشارة وبلا رابط. مهاجرة **0025** (عمود `is_upcoming`، إضافة بحتة، `IF NOT EXISTS`) طُبِّقت على الإنتاج بعد نسخة احتياطية (4.6م) — السجل 25→26. مفتاح «ضيف قادم» في `/admin/home-content`. commit `6909496` · ٢٩٢٩ اختباراً · على اللايف: ٥ صور دائرية محمّلة، «الضيوف» قبل «أحدث الحلقات» | — | ✅ |
| 2026-08-11 | ↳ ثلاث غلطات مني أُمسكت قبل النشر: dedup كان يُسقط النسخة **المعلَّمة** لا التلقائية · `ring-dashed` فئة مخترعة (الإطار طلع صلباً) · `scrollbar-none` غير موجودة والصحيحة `scrollbar-hide` | — | ✅ |
| 2026-08-11 | ✅ **دفعة ١ منشورة ومتحقَّقة** — `م-١٢` (تباعد الحروف · ٤ أيقونات PNG مستضافة · روابط المنصات من `podcast_platform_links`) · `م-١٣` (خط Manifa في القالبين عبر `@font-face`) · `م-٨` (اسم البطاقة → «راعي الحلقة») · `م-٣` (المرساة الفاضية `sec-takeaways`). commit `0928839` · ٢٩٢٦ اختباراً · الأيقونات والخط 200 على اللايف · المرساة اختفت | — | ✅ |
| 2026-08-11 | ↳ **م-١٤ (ملاحظة جديدة — نهاية صفحة الحلقة)** من لقطة خالد: «اكتشف أكثر / تأملات مرتبطة» + «الحلقة السابقة/التالية» فوق «حلقات لها علاقة» = ثلاث كتل تنقّل متراكمة. **تحقّق**: السابقة/التالية **تعمل بشكل صحيح** (018 → 017 / 019، محصورة بالحارة) — فالحجة تصميمية لا عطل: مكرِّرة وأضعف من «حلقات لها علاقة» تحتها (عناوين مقصوصة بلا صور)، وأرشيف خط ليس متسلسلاً سردياً. **تصحيح لخالد**: التأملات **مرتبطة فعلاً** بـ`episode_id` — لكنها **بلا أي واجهة إدارة** (٤١ صفاً غير قابلة للتحرير أو الاعتماد أو الحذف)، والبطاقة تنتهي بسؤال بلا رابط، ونصف «اكتشف أكثر» الآخر (`home_quotes`) **صفر صفوف**. **توصيتي: حذف القسمين** ⇒ النهاية تصير الراعي ← الموضوعات ← حلقات لها علاقة. عودة التأملات لاحقاً **مشروطة بمحرّر** | خالد | ⏳ قرار |
| 2026-08-11 | ⭐ **دفتر ملاحظات خالد — قيد الجمع.** خالد يعطي ملاحظاته تباعاً، ثم تُرتَّب بالأولوية، ثم يبدأ التنفيذ. **ممنوع التنفيذ قبل اكتمال الجمع والترتيب وموافقته** | — | ⏳ يجمع |
| 2026-08-11 | ↳ **م-١ الاقتباسات** — ٤٣ اقتباساً في ٧ حلقات: حالة الصف `published` محلياً منذ **فبراير ٢٠٢٦** و`draft` على الإنتاج ⇒ القسم غير ظاهر على أي حلقة. أسرع مكسب في القائمة. ⚠️ تنبيه قائم: بعض المنسوب للضيف مولَّد لا منقول — يُراجَع قبل النشر | — | ⏳ |
| 2026-08-11 | ↳ **م-٢** `guest_testimonial` و`guest_video_url`: القسم يقرأهما ويعرضهما و**لا كاتب لهما في المشروع كله** — عمودان ميتان يغذّيان قسماً لا يمكن تعبئته | — | ⏳ |
| 2026-08-11 | ↳ **م-٣** `<div id="sec-takeaways">` يُرسم فاضياً على الـ٤١ حلقة (نفس العيب المُصلَح لـ`sec-why` ولم يُطبَّق هنا) · و**ستة مراسي `sec-*` بلا أي رابط** في المشروع — فهرس محتوى مفقود أو محذوف | — | ⏳ |
| 2026-08-11 | ↳ **م-٤ بقايا ميتة**: جدولا `timestamps` و`resources` — صفر كاتب وصفر صف، ومسار يوتيوب يبنيهما من `episode_enrichments` · و`episodes.summary` بلا كاتب رغم أن الصفحة **تقرأه أولاً** | — | ⏳ |
| 2026-08-11 | ↳ **م-٥ كتابة مزدوجة**: الدفعة تكتب `pkg.full_summary` في `episode_overrides.custom_description` **و** في `episode_enrichments.full_summary` — مصدران لحقيقة واحدة، وهو أصل عطل ٢٠٢٦-٠٨-١٠ | — | ⏳ |
| 2026-08-11 | ↳ **م-٦ أربع بوابات نشر متضاربة** على صفحة واحدة: الإثراء (افتراضه `published`) · «ما لم يُقال» (اعتماد ببند، افتراضه منع) · الاقتباسات (افتراضها مسودة) · الإخفاء. وافتراض `publish_status='published'` يعني **دفعة Studio تنزل بلا مراجعة** — يُحسم قبل تشغيل Studio فعلياً | — | ⏳ |
| 2026-08-11 | ↳ **م-٧ الحدّ بين اللوحة والاستوديو غير مرئي**: ١١ حقلاً عاماً — ٦ تُحرَّر من تبويب «إثراء الحلقة» و٥ (`hero_summary`/`full_summary`/`takeaways`/`resources`/`timestamps`) **مسارها الوحيد دفعة Studio بلا أي محرر يدوي**، ولا قاعدة تُميّز أيّها أين. **المقترح: صفحة الحلقة تعرض الـ١١ كلها، الخمسة للقراءة فقط مع مصدرها ورابط «افتح في الاستوديو» وحالة آخر دفعة** — تغيير واجهة لا معمارية | — | ⏳ |
| 2026-08-11 | ↳ **م-٨** «شريك الحوار» في صفحة الحلقة **ليست شريك حوار — هي مُحدِّد الراعي** (Sponsor Card). تسمية مضللة | — | ⏳ |
| 2026-08-11 | ↳ **م-٩ تشتّت التحرير**: الموضوعات تُحرَّر من `/admin/home-content` لا من صفحة الحلقة · و`daily_reflections` (٤١ صفاً تظهر في «اكتشف أكثر») **بلا أي واجهة إدارة** | — | ⏳ |
| 2026-08-11 | ↳ **م-١٠ تمييز لازم**: ٩ من الحقول فاضية لأن الـ٤١ حلقة **مستوردة من يوتيوب وما مرّت على Studio قط** (٤ سجلات EIR · ٢ تحليل · ٠ تيزر). **شغل محتوى لا هندسة — إعادة التنظيم لن تملأ الصفحة** | خالد | ⏳ |
| 2026-08-11 | ↳ **م-١٣ (سؤال خالد → ملاحظة)** — **الإيميلات ليست بخط الهوية**: الموقع `Manifa V2` والقوالب `'Segoe UI', Tahoma, Arial` في ٤ مواضع. `Segoe UI` خط **ويندوز** فلا يصل قارئ آيفون أصلاً. **الحد الحقيقي**: Apple Mail يحترم `@font-face` فتُحمَّل Manifa فعلاً، بينما **Gmail يجرّده** وOutlook لا يرندره. الخطوط مخدومة عامّاً أصلاً (`/fonts/manifa-v2-400.woff2` → 200، ٢٦ك). النصف الثاني من الإصلاح: السلسلة الاحتياطية يجب أن تبدأ بخط النظام العربي لا بخط ويندوز | — | ⏳ |
| 2026-08-11 | ↳ **م-١٢ (طلب جديد — بريد النشرة)** من لقطة خالد لرسالة الترحيب: (أ) **«نشرة خط» بحروف مفصولة** — السبب `letter-spacing:1.5px` على نص عربي في `lib/email/templates.ts:310`؛ التباعد يكسر وصل الحروف. **٥ مواضع بنفس العلة** (منها «رقمك المرجعي» في ٤ قوالب) — والتباعد على رقم المرجع نفسه **سليم** لأنه لاتيني · (ب) **أيقونات التواصل رموز يونيكود عامة لا شعارات**: إنستغرام `&#9679;` ● · يوتيوب `&#9654;` ▶ · تيك توك `&#9836;` ♬ · إكس `&#120143;` — تتغير بين عملاء البريد حسب الخط. **الحل المتسق مع الملف نفسه: PNG مستضافة** (Gmail يجرّد SVG المضمّن وOutlook لا يرندره — نفس سبب استضافة الشعار). حجم الشغل صغير | — | ⏳ |
| 2026-08-11 | ↳ **م-١١ (طلب جديد — الرئيسية)**: شريط ضيوف أفقي بدل دمجهم مع الحلقات — صور **دائرية** + اسم الضيف، تمرير يمين/يسار، **وتحكّم يدوي** لإضافة ضيوف الموسم الثاني كـ«قريباً/تشويق» قبل نزول حلقاتهم | — | ⏳ |
| 2026-08-11 | ↳ **قرارا خالد على م-١١**: (١) ضيوف «قريباً» **لهم شكل مختلف** عن الضيوف المنشورين — التفاصيل البصرية تُقترح عند التنفيذ · (٢) **الشبكة تصير حلقات فقط** ويُرفع الشريط **فوقها** ⇒ يُحذف `interleaveGrid` من الرئيسية. ⚠️ للتنفيذ: ضيف بلا حلقة — إلى أين يشير؟ صفحة الضيف ستكون فارغة ⇒ الأرجح بلا رابط | خالد | ✅ محسوم |
| 2026-08-11 | ↳ حقائق مقاسة لـ**م-١١**: الضيوف اليوم مدموجون في شبكة الحلقات (خلايا ٣/٥/٧ عبر `interleaveGrid`) · **١٦ من ٢٠ ضيفاً عندهم صور على الإنتاج** (تعليق الكود «٠ من ٢٠» **قديم**) · `homepage_thinkers` = **٠ صفوف** ⇒ الوضع تلقائي · الوضع اليدوي يشترط وجود الضيف في `guests` لكنه **لا يشترط حلقة** ⇒ «قريباً» ممكن بلا حلقة · **لا يوجد حقل «قريباً»** — يحتاج عموداً جديداً أو إعادة استخدام `custom_title` | — | ⏳ |
| 2026-08-08 | ⭐ **عملية البوليش** — النطاق مُعرَّف: **جولة أ = الموقع العام + بوابات البناء فقط**. اللوحة **مؤجَّلة لجولة ب** بطلب منفصل (السبب: بلا قائمة شاشات من خالد فهي بلا حدود). ٧ معايير قبول + قائمة خارج-النطاق | mariam | ✅ مُسلَّم — بانتظار «ابدأ» |
| 2026-08-08 | ↳ 🔴 **حاجز أول**: `RESEND_WEBHOOK_SECRET` ناقص محلياً و`prebuild` يشغّل `validate-env --strict` ⇒ **`npm run build` يفشل محلياً** ⇒ بوابة التحقق كلها ساقطة. يُحلّ قبل أي شغل بوليش | خالد | ⏳ قرار |
| 2026-08-08 | ↳ مراجعات متوازية: سارة (شكل + RTL + جوال) · نورة (QA اللوحة) · يوسف (أمن + قاعدة) — **مريم لا تكرر قياساتهم، تستهلكها كدليل** | — | ⏳ |
| 2026-08-03 | ⭐ **حالة المشروع: `55fe850` — ٢٥ كوميت، كلها منشورة.** الإنتاج `BUILD_ID 9kS7gfgexaH-jBsiKQE46`. **لا شيء ينتظر النشر** | — | ✅ |
| 2026-08-03 | ↳ **مطابقة live ↔ local مُثبتة ببصمة**: `40aab5e712a309d8e6e8add7c449d9ef` على الشجرتين · ١٢٨ جدولاً · ٢٤ مهاجرة · ٣ ضيوف · ٧ إثراءات — متطابقة. **الفرق الوحيد ٤٢ محلياً مقابل ٤١**: صف `smoke-ux3b` محلي ولم يصل الإنتاج قط | noura | ✅ متحقَّق |
| 2026-08-03 | **تدقيق تصميم بثلاثة مراجعين للموقع العام** → ٤/١٠. ثم الهوية والأساس والأرشيف والإصلاحات → **سارة أعادت التقييم على الحيّ: ٦٫٥/١٠** | omar | ✅ منجز |
| 2026-08-03 | ↳ **الشعار المنشور كان نسخة CSS مقلّدة بألوان مخترَعة** — وتصل كل مشترك في النشرة عبر `lib/email/templates.ts`. استُبدل بفنّ SVG مستخرج من ملف الهوية | fahad | ✅ منشور |
| 2026-08-03 | ↳ **الأرشيف ثلاث حارات** (خط/سالفة/مقاطع) بقاعدة **معكوسة عمداً** في `lib/episodes/programs.ts`: التصنيف موسمٌ من خط **إلا** المستثنى — فالموسم الثاني يدخل بأي إملاء بلا كود | fahad | ✅ منشور |
| 2026-08-03 | ↳ 🔴 **فهرس الحلقة كان مخترعاً**: النص يصل بـ٢٨٦٩ توقيتاً حقيقياً وتُرمى، والأزمنة تُستوفى بعدد الأحرف. وُصل بمسار المقاطع الموقّتة الموجود أصلاً. **الوسيط 189.9s→2.6s · انحياز النصف الأول +317.7s→−1.4s · النداء أرخص ٧٦٪** | fahad + rashid | ✅ منشور |
| 2026-08-03 | ↳ **خمسة مولّدات جديدة** لحقول «الحوار» (كانت تُرسم عامةً بلا مولّد). `exclusive_clip` **بلا مولّد عمداً** — يضمّن رابط يوتيوب ولا يُخترع رابط لفيديو موجود | fahad | ✅ منشور |
| 2026-08-03 | ↳ **بوّابة مراجعة لـ`unsaid_reflections`**: الافتراضي منع، والموافقة **بالنص لا بالفهرس** (تعديل الصياغة يُسقط الاعتماد، وحذف بند لا ينقله). نورة حاولت كسرها **٢٥/٢٥ وما طاحت** | fahad | ✅ منشور |
| 2026-08-03 | ↳ **التوليد محصور بحلقات خط الكاملة** — قرار خالد: سالفة والمقاطع لا تستحق القوالب. عبر `laneOfEpisode`، في مكان واحد | fahad | ✅ منشور |
| 2026-08-03 | ↳ **انحدار أدخلناه وأصلحناه**: `duration = 0` تعني «فشل الجلب» لا «صفر ثانية»؛ إصلاح سابق قلبها من «لا يفحص» إلى «يرفض كل شيء». القاعدة صارت مكتوبة مرة واحدة في `normalizeDurationSeconds` | fahad | ✅ منشور |
| 2026-08-03 | 🔴 **باقٍ على خالد**: إعادة توليد محتوى كل الحلقات (**الفهارس المنشورة ما زالت مفبركة ٧/٧، اثنتان تشيران بعد نهاية الفيديو**) · أسماء الضيوف وصورهم · روابط المنصات لـ`/listen` · **ولم يضغط أحد زر التوليد قط** (تسجيل الدخول يعني كتابة كلمة مرور، وهي ممنوعة علينا) | خالد | ⏳ |
| 2026-08-03 | 🟡 **باقٍ علينا وغير مجدول**: التوصيات «الأحدث في الحارة» لا ترابطاً حقيقياً · soft-404 على المسارات الديناميكية (٢٠٠ على بناء إنتاجي) · `Failed to find Server Action` **ضجيج يومي مزمن منذ ١١ يوليو (٢٥–١٦٢/يوم) لا أثر نشر** · ٦٣/١٦٥ server action بلا `try/catch` · مسار النص الخام (**أرخص ٢٨٫٦٪ بثبات متعادل** — يوصي به راشد) | — | ⏳ |
| 2026-07-31 | **فحص شامل لرحلة إعداد الحلقة** (من إنشاء الـEIR إلى غرفة التسجيل) — مُسلَّم. الحكم **٤/١٠**: المحرّك ٧/١٠ والرحلة حوله ٢/١٠. **صفر تعديل مصدر أثناء الفحص** | omar | ✅ منجز |
| 2026-07-31 | ↳ **الموجة ١ + ١ب** — ١٣ إصلاحاً (تصنيف الأخطاء · تحذير `to-preparation` · `insight_stats` داخل JSONB · أسباب التحقق بالعربي · ٧ أزرار على `run-action` · قفل `FOR UPDATE` · ختم `subject` · fallback الضيف · التباين · لوحة تأكيد). **local فقط · صفر نشر · صفر migration · صفر عمود** | fahad | ✅ منجز ومُتحقَّق |
| 2026-07-31 | ↳ QA مستقل: ١٠/١٣ صمدوا أول جولة · **٢٦ mutation، ١٦/١٨ قُتلت** · التباين **1.02:1 → 8.95:1** مقيس · صفر انحدار retry أمام ٢٠ رسالة مزوّد حقيقية. البوابات: **2142 اختبار · tsc نظيف · lint exit 0 · build ✅** | noura | ✅ منجز |
| 2026-07-31 | ↳ **الموجة ٢ (الصحّة)**: بوابة التبويبات · تناقض `phase↔guest` · labels البطاقات · مسح شامل لنمط `startTransition` · تغطية تحذير `bulk-convert` | fahad | ⏳ قيد التنفيذ |
| 2026-07-31 | ↳ **الكنسة البصرية كوحدة واحدة** بعد الموجة ٢ — المراجعة **بالقياس لا بالكلاسات** | sara | ⏸ ما بدأت |
| 2026-07-30 | **صفحة التسجيل اللايف + صفحة المخرج + أثر الطاقة** — ٤ مراحل + ٣ نشرات على الإنتاج (`23a0d5a` · `28c8b5a` · `e78d280`)، ledger **23/23**، ولا بند معلّق | omar/fahad/noura | ✅ منجزة ومنشورة |
| 2026-07-29 | **إظهار التصنيفات في الموقع العام** — خالد وافق على الثلاث قطع: (١) شارة تصنيف على الكرت وصفحة الحلقة (٢) فلتر `?category=` في `/episodes` (٣) صفحات `/categories/[slug]`. معايير القبول مُسلَّمة (١٥ معيار) + ٥ قرارات موقوفة على خالد. **local فقط — لا نشر** | mariam | ⏳ بانتظار قرارات خالد ثم «نفّذ» |
| 2026-07-29 | ↳ المراجعة البصرية للشارة/الفلتر/صفحة التصنيف (RTL) | sara | ⏳ بالتوازي |
| 2026-07-27 | **خطة الإصلاح الشاملة** — مُسلَّمة: `.claude/studio-fix-plan-2026-07-27.md` (تحقق مستقل من 3 ادعاءات حرجة · جدول مطابقة كامل لكل ادعاء في تقرير الفحص · 28 بند إصلاح في 6 موجات بمعايير قبول من محاور مريم السبعة · تصحيح التكلفة بأرقام مقيسة · 9 قرارات لخالد). **صفر تعديل على كود المصدر** | omar | ✅ منجز — بانتظار «نفّذ» من خالد |
| 2026-07-27 | ↳ **تشغيل حي بميزانية $8 معتمدة** — حلقة `ZPeBeS87EeI` (3س36د)، الجلسة `09f31ded`: **48 نداء · $2.5996 · ~30 دقيقة**. أطول حلقة في القاعدة، الخط الثماني كامل | noura | ✅ منجز |
| 2026-07-27 | ↳ نموذج تكلفة بأسعار متحقق منها حياً + اكتشاف `buildTimedSegmentsFromVtt()` (مبنية ومختبَرة وصفر استدعاء إنتاج) + `prompt caching` يقلب ق-1 من كلفة إلى توفير | rashid | ✅ |
| 2026-07-27 | ↳ تثبيت بندَي الأمان + اكتشاف أن `speaker` لا يصل الموقع العام إطلاقاً (كل اقتباس يُنسب للضيف بالاسم والصورة) وأن حافة `finalized → published` مبنية وفاضية | yousef | ✅ |
| 2026-07-27 | **فحص الاستوديو الشامل** — التقرير النهائي مُسلَّم: `.claude/studio-audit-2026-07-27.md` (10 أقسام: خلاصة، شرح بلغة خالد، حكم المحاور السبعة، 11 عطلاً، خطة 6 موجات، 8 قرارات مطلوبة، ما لم يُفحص، ملحق تقني). **صفر تعديل على كود المصدر — القيد احتُرم بالكامل** | omar | ✅ منجز — تجاوزته خطة الإصلاح |
| 2026-07-27 | ↳ تحقق عمر المستقل من 5 ادعاءات متصدّرة + 4 إضافية: 7 صمدت، **2 لم تصمد** (توقيع الفصول/المقاطع ما فيه وسيط intelligence أصلاً → الجهد أكبر من تقدير راشد؛ «40 من 41 حلقة تنضغط» غلط → الصحيح **19 من 19 حلقة كاملة**، جدول episodes ثنائي القمة) | omar | ✅ |
| 2026-07-27 | ↳ المحاور 1–6 (كود · AI · QA · UX/RTL · أمن · توزيع) + تعريف «أعلى جودة» — كلها مُسلَّمة داخل التقرير | fahad/rashid/noura/sara/yousef/marzouq/mariam | ✅ (محور QA جزئي: 9 من 11 مخرج ما تولّدت) |
| 2026-07-18 | فحص اللوحة الشامل + المرحلة 1 (6 مهام) + ثغرة 1-5 + التيزر v1 — كلها أُغلقت رسميًا بعد QA نورة المستقلة | omar/fahad | ✅ منجزة |
| 2026-07-25 | إعادة تصميم `/admin/ops` — Wave 1 منشور على الإنتاج (`ba2d918`) | — | ⏳ Wave 2 ما بدأ |
| 2026-07-25 | `teaser_questions` — مواصفة مقفلة (عدّاد بالوارد + `/admin/teaser-questions`، بلا migration) | — | ⏸ ضمن Wave 2 |
| 2026-07-19 | قسم دعوة الضيف — خطة + 8 معايير قبول جاهزة | — | ⏸ بانتظار «نفّذ» من خالد |
| 2026-07-21 | تعريب taxonomy السوق + قياس الزخم (يحتاج snapshots زمنية) | — | ⏸ ما بدأت |
| 2026-07-18 | المرحلتان 2 و3 من خطة اللوحة | fahad | ⏸ ما بدأت |

## تعريف «أعلى جودة» للاستوديو (mariam، 2026-07-27 — مرجع الحكم النهائي)

سبعة محاور، كل محور له مقياس ومصدر رقم وخط أساس. القياس على **عيّنة 3 حلقات حقيقية** لا حلقة وحدة.

| # | المحور | المقياس | خط الأساس المقبول |
|---|---|---|---|
| ج1 | دقة النص المستخرج | `transcript-quality.ts`: maxConsecutiveRun · uniquenessRatio · إشارات intra-segment + مقارنة 3 نوافذ ×60ث مع الصوت | صفر verdict «degenerate» يعدّي · uniqueness ≥0.85 · صفر خطأ اسم عَلَم بالنوافذ |
| ج2 | أمانة المحتوى (لا هلوسة) | كل timestamp/اقتباس/اسم بالمخرجات يُطابق نصيًا على الـ transcript | **صفر تسامح**: 100% من الأرقام والاقتباسات مُثبتة، وإلا المخرج مرفوض |
| ج3 | جاهزية النشر | نسبة الحقول اللي عدّلها خالد قبل النشر (قياس مباشر على حلقة) | ≥70% من الحقول تُنشر كما هي · صفر حقل يحتاج إعادة توليد كاملة |
| ج4 | سلامة العربية | حكم خالد الأعمى على 10 مقتطفات (مو حكم موديل — أثبتنا 2026-07-26 إن حكم الموديل للعربي بلا قيمة) | صفر إنجليزي مُقحَم · صفر ترجمة حرفية · اللهجة مطابقة لصوت البودكاست |
| ج5 | القوة التسويقية | فحص قواعد (طول العنوان، هوك بأول 15ث، بنية الوصف، عدد المقاطع) + حكم مرزوق مقابل سلوك المنصات 2026 | 100% مطابقة القواعد · ≥3 مقاطع صالحة للنشر بلا قص يدوي |
| ج6 | سرعة الإنتاج | `ai_runs.latency` مجمّعة على `subject_id` + زمن الحائط من الرفع لجاهز-للنشر | ≤45 دقيقة لحلقة ساعتين · صفر مرحلة تعلّق بلا رسالة خطأ |
| ج7 | التكلفة/الحلقة | `SUM(cost_usd)` من `ai_runs` حيث `subject_table='studio_sessions'` — شامل Whisper | رقم واحد معلن ومُثبت لكل حلقة · صفر استدعاء AI خارج الراوتر (غير محسوب) |

## محاور نجاح رحلة الإعداد (mariam، 2026-07-31 — مرجع الحكم على كل موجة)

سبعة محاور. القياس على **بيانات القاعدة الفعلية** لا على قراءة كود، وخط الأساس اليوم مكتوب صراحةً حتى يُقاس التحسّن لا يُدَّعى.

| # | المحور | المقياس | خط الأساس اليوم → المقبول |
|---|---|---|---|
| ر1 | وحدة الرحلة | عدد نقاط إنشاء الـEIR · عدد أنظمة التوليد على نفس الصف | **٥ نقاط · نظامان** (`prep_v2` JSONB مقابل الأعمدة العشرة) → نقطة كتابة واحدة معلومة، ونظام واحد أو رابط صريح بينهما |
| ر2 | صدق المرحلة | `eir_phase_transitions`: نسبة التحضيرات اللي وصلت `prepared` بانتقال مسجَّل | **٠ من ٣٥ صفاً وجهته `approved`/`researching`/`prepared` · ٨/٨ تحضيرات `draft`** → كل تحضير مكتمل له انتقال مسجَّل، والـtrigger يحسمه خالد (اليوم **REPORT** لا `enforce`) |
| ر3 | تماسك هوية الضيف | `eir.guest_id` غير NULL للحلقات المسنَدة · تطابق ما تعرضه الصفحة مع القاعدة | **٨/٨ بـ`guest_id = NULL`** والفلتر «بدون ضيف» عديم القيمة → كاتب آلي واحد + قارئ بلا تناقض. (`linked_episode_id` عمود ميت — **ليس** الجذر) |
| ر4 | وصول الإسناد | عدد البطاقات المحفوظة / المرشّحة، وكلفة الإسناد كنسبة من كلفة التحضير | **٦/٦ تحضيرات بصفر بطاقة، والإسناد ٦٦٪ من الكلفة** → صفر كلفة بلا مخرج؛ أي تمريرة تُنتج صفراً **توقف وتُعلن**، لا تُحتسب |
| ر5 | أمانة السؤال | نسبة الأسئلة اللي وصلت خالد بنصّها المولَّد (معرّف غير `crit-` بلا سبب) | **١١ من ٢٨ أعاد النقّاد تأليفها** بسبب `slice(0, 14_000)` في `critique.ts:117` → **صفر سؤال يُعاد تأليفه بحادث**؛ أي إعادة تأليف مقصودة ومرئية |
| ر6 | ممنوع الصمت | كل فشل يصل الشاشة بسبب صحيح (تصنيف الخطأ · `ok` صادق · لا شاشة تختفي وهي فاضية) | خطأ Gemini كان يُصنَّف `rate_limited` فيُعاد ٣ مرات (**١٨٠ صفاً فاشلاً**) والتنبيه ما ينطلق أبداً → **صفر فشل صامت**: التصنيف صحيح، والفراغ يُشرح لا يُخفى |
| ر7 | كلفة الاحتكاك | عدد الضغطات الإلزامية من إنشاء الحلقة إلى «جاهزة للتسجيل» + كلفة التحضير المعلَنة | **١٢ ضغطة** (٦ ضرورية · ٣ تُدمج · ٣ تُحذف) و**$1.2311/تحضير** → ≤٧ ضغطات، ورقم كلفة واحد معلَن ومُثبَت لكل تحضير |

## القرارات

- 2026-08-08 — **تعريف «بوليش» لهذا المشروع (mariam) — مرجع الحكم على كل بند**: البوليش = **إصلاح خلل في شيء مشحون فعلاً**، على سطح يلمسه زائر أو خالد، **بلا ميزة جديدة ولا جدول ولا عمود ولا migration ولا dependency ولا إعادة تصميم صفحة**. المقياس: «شيء مكسور صار صحيحاً» — **وليس** «شيء صار أحلى». أي بند لا يمرّ هذا التعريف يُرفض بالافتراض ويُرفع لخالد.
- 2026-08-08 — **حدود نطاق البوليش (mariam)**: **داخل** = المسارات العامة (`app/(site)/**`, `app/episodes/**`, `app/guests/**`, `app/topics/**`, `app/about`, `app/listen`) · الحالات الفاضية والأخطاء بالعربي · RTL/الجوال ضمن التوكنز القائمة · النماذج العامة وردّ فعلها · **بوابات البناء الأربع**. **خارج ويُرفض**: الألوان والخطوط (**أمر خالد الدائم**) · `dark:` (مؤجل بقراره لمشروع مستقل) · صور الضيوف والمحتوى الفاضي (شغل خالد بعد الشحن) · الاستوديو ورحلة الإعداد وdiscovery (لها خططها: **٤/١٠** والموجة ٢ معلّقة) · `/admin/**` كله في هذي الجولة · DMARC ورصيد X API · أي نشر أو migration أو عملية بيانات.
- 2026-08-08 — **الأولوية: الموقع العام قبل اللوحة** — بثلاثة أسباب لا تفضيل: (١) الموقع **حيّ والصيانة انشالت اليوم** فكل زائر يشوف الخلل الآن، واللوحة **مستخدمها واحد** يعرف التفافاتها؛ (٢) اللوحة عليها **خطتان مفتوحتان** (موجة ٢ + رحلة الإعداد ٤/١٠) و«بوليش عام» فوقهما يتصادم معهما ويكرّر شغل نورة؛ (٣) اللوحة بلا **قائمة شاشات من خالد** سطح بلا حدود (~١٠٨ جداول) — فهي **جولة ب بطلب منفصل**، وإلا تُرفض.
- 2026-08-08 — 📌 **حقيقتان مثبَّتتان تغيّران ترتيب المعلّقات**: (١) `ADMIN_NOTIFY_EMAIL` **ليس عطلاً** — الكود فيه fallback مكتوب `khatpodcast@hotmail.com` في ٣ مواضع (`guest-application/route.ts:214`, `sponsor/route.ts:153`, `partner-task-reminder.ts:27`)، فالرسائل تصل؛ السؤال **إعدادي لخالد** لا إصلاح. (٢) `RESEND_WEBHOOK_SECRET` **حاجز حقيقي**: `severity:"recommended"` لكن `prebuild` يشغّل `--strict` (`validate-env.ts:215`) فيسقط البناء ⇒ **معيار «`npm run build` ينجح» غير قابل للفحص أصلاً قبل حلّه**.
- 2026-08-03 — **قرارات خالد في جلسة التصميم — لا تُعاد مناقشتها**: القوالب لحلقات خط الكاملة **فقط** (سالفة والمقاطع خارجها) · `exclusive_clip` **ملغى** · `who_is_it_not_for` **يبقى** · عناوين الأقسام **فصحى** (والنبرة داخل النص تبقى دافئة) · `unsaid_reflections` **بمراجعته لكل حلقة** · **الروابط تبقى عربية** (ألغى تحويلها للإنجليزية صراحةً) · **لا يُخفض** «الضيوف» ولا «استمع» من التنقّل · **لا يُستخرج** الفهرس البشري من وصف يوتيوب (إعادة التوليد تغطّيه).
- 2026-08-03 — **تقسيم العمل الذي أعلنه خالد**: التصميم والبرمجة علينا · **أسماء الضيوف وربطهم وصورهم وتوليد محتوى كل صفحات الحلقات عليه هو**، بعد أن ننتهي ونرفع وقبل الإعلان الرسمي. ⇒ **لا نُوقف العمل انتظاراً لبياناته، ولا نحكم على صفحة فارغة كأنها الحالة النهائية.**
- 2026-08-03 — **يوتيوب مرجعٌ للفيديو وغلافه**: `thumbnail_url` و`duration_minutes` و`view_count` تأتي من يوتيوب دائماً وتتجاهل القاعدة — **وهذا مطابق لنيّة خالد**، فلا يُقترح غلاف مخصّص.
- 2026-08-03 — ⚠️ **ممنوع تشغيل `scripts/seed-official-platforms.ts` على الإنتاج**: روابطه مخمّنة وأربعة منها ترجع أخطاء. خالد سيرسل الروابط الحقيقية.
- 2026-08-03 — **درسان في النشر** (في [[deploy-runbook-droplet]]): حين تُحذف ملفات محلياً **يجب إزالتها على الخادم بأسمائها** — ملف مهجور أسقط البناء كله لأنه يستورد دالة محذوفة، **والفشل صامت من ناحية الزائر** (البناء القديم يستمر) · و**`data/` على الخادم ٢٧٢MB من حالة تشغيل** (`cookies.txt` لـyt-dlp، الصوتيات) — تُستثنى من النقل دائماً.
- 2026-08-03 — **قاعدة اختبارية مكتسبة**: الحارس يَعمَى بدل أن يفشل. مجموعة خضراء ليست دليلاً؛ الدليل هو حارس مُثبَت البصر بطفرة **باتجاهين**: اكسر المنتج وانتظر فشلاً **بالاسم**، ثم أعمِ الماسح وانتظر الشبكة. التفاصيل في [[guards-that-go-blind]].

- 2026-07-31 — 📌 **حقائق مثبَّتة من فحص رحلة الإعداد** (مقيسة على القاعدة، تُعامَل كخط أساس لا كرأي): **٥ نقاط دخول تُنشئ EIR** (`seasons/actions.ts:790` · `to-preparation.ts:123` · `studio-resolver.ts:63` · `guest-candidates/production-bridge.ts:124` · `guest-crm/production-bridge.ts:71`) · **نظاما توليد متوازيان بلا رابط** على نفس الصف: `prep_v2` JSONB (khat-brain + غرفة التسجيل) والأعمدة العشرة القديمة (`/admin/preparation/[id]` وحدها) · `eir_phase_transitions` **٣٥ صفاً وصفر وجهته `approved`/`researching`/`prepared`** · **٨/٨ تحضيرات `draft`** · **٨/٨ EIR بـ`guest_id = NULL`** · **٦/٦ بصفر بطاقة إسناد**. الحكم **٤/١٠** — المحرّك ٧/١٠ والرحلة حوله ٢/١٠.
- 2026-07-31 — 🔴 **الجذر الحقيقي لصمت الإسناد (يُلغي تشخيص أمس الجزئي)**: `lib/ai-router/errors.ts` كان يفحص كلمة `quota`، ورسالة Gemini `"exceeded its monthly spending cap"` + `RESOURCE_EXHAUSTED` **ما فيها الكلمة** ⇒ تُصنَّف `rate_limited` ⇒ ٣ محاولات لكل مرشّح (**١٨٠ صفاً فاشلاً**) و**تنبيه «المزوّد أوقف خدمتنا» ما ينطلق أبداً**. أي تصنيف خطأ مبني على مطابقة كلمة **يُعتبر عطلاً محتملاً** حتى يُمرَّن على رسائل مزوّد حقيقية.
- 2026-07-31 — 🔴 **`critique.ts:117` كان يعيد تأليف الأسئلة بحادث**: `slice(0, 14_000)` يقصّ الـJSON في منتصف كائن — البتر يبدأ عند **السؤال ٢٠ من ٢٨** ⇒ **١١ من ٢٨ سؤالاً معرّفها `crit-`** (النقّاد ألّفها لا المولّد). خالد أكّد الرقم من نسخته الاحتياطية. + **`temperature` مرمي بصمت في ١٢ موضعاً** (`providers/openai.ts:89` — كل موديلات الرحلة reasoning) فأي ضبط عليه اليوم **وهم**.
- 2026-07-31 — **تصحيح تشخيص تناقض الضيف**: `linked_episode_id` **عمود ميت** بلا كاتب إنتاجي و**ليس** الجذر. الجذران: (أ) ما فيه كاتب آلي لـ`eir.guest_id` (ب) القارئ بلا fallback. ⇒ الإصلاح المشحون هو fallback القراءة؛ الـbackfill قرار خالد (ق٧).
- 2026-07-31 — ✅ **الموجة ١ + ١ب منفَّذة ومُتحقَّقة — local فقط، صفر نشر، صفر migration، صفر عمود**: ١٣ إصلاحاً، منها إصلاح تصنيف الأخطاء من الجذر · fallback الضيف · `insight_stats` داخل الـJSONB · ٧ أزرار انتقلت لـ`run-action` · قفل `FOR UPDATE` · التباين **1.02:1 → 8.95:1** مقيس. تحقق نورة: **١٠/١٣ صمدوا أول جولة**، و**٢٦ mutation سوّتها بنفسها و١٦/١٨ قُتلت**.
- 2026-07-31 — ⚑ **سلوك يُسجَّل كمطلوب لا كخطأ (أمر خالد)**: فهد **أفصح من نفسه** عن كسره خادم الـdev مرتين قبل ما يُسأل، وحدّد النافذة (**٢٠:١٣–٢٠:٢٣**) وقال إن قياسات سارة ونورة داخلها تُعاد. **الإفصاح الذاتي عن الضرر الجانبي هو المعيار** — لا إخفاؤه ولا انتظار السؤال.
- 2026-07-31 — 🔴 **درس: اختبار أخضر فوق ميزة ميتة**. `tests/db-mock.ts` **يتجاهل الـprojection** — حذف `guest_name` من الـSELECTين ما أفشل شيئاً، فـ**٢١٤٢ اختباراً بقيت خضراء**. دَين بنيوي: **خالد يطرحه بنفسه، وممنوع ترقيعه بالمرور**. ومعه: **٣ اختبارات فاضية** كشفها الـmutation (أحدها كان أخضر على الباگ نفسه) ⇒ أي اختبار «يثبت» شيئاً لازم يُثبَت بإرجاع الإصلاح.
- 2026-07-31 — **تصحيح واقعي**: `npm run lint` **ما عاد يفشل** (exit 0 · 39 warnings). وصف «فشل بيئة على `react-hooks`» **بطل ولا يُنقل لأي تقرير**.
- 2026-07-31 — 🟠 **حكم يوسف على تجاوز بوابة التبويبات بالـURL**: البوابة هي **الحماية الوحيدة على مستوى المرحلة** (١٥/١٥ action ما تقرأ `eir.phase`)، **بس مو تجاوز صلاحية**. منفذان مسجَّلان: `episode_overrides` بلا publish gate · `refreshYoutubePerformanceAction` يختم الحلقة **`published` بلا نشر** ويلوّث سجل التدقيق **بلا رجوع** (سلوكه = ق٨).
- 2026-07-31 — **٩ قرارات موقوفة على خالد**: ق١ سقف Gemini · ق٢ حسم الانتقالات (الـtrigger اليوم **REPORT**، والوحيد اللي يضبطه `enforce` سكربت اختبار) · ق٣ تأريض Pass 1 · ق٥ صرف مدفوع · **ق٦ أرشفة قبل الكتابة** (الخطر الفوري **مغطّى** — النسخة الاحتياطية عند خالد في `~/Desktop/khat-backups/`) · ق٧ backfill لـ`eir.guest_id` · ق٨ سلوك `syncEirOnPerformanceWrite` · ق٩ نص فلتر «بدون ضيف» (اليوم **عديم القيمة — ٨/٨ تحته**).
- 2026-07-30 — ⚑ **توضيح خالد يقفل النطاق نهائياً**: «الأسئلة تم توليدها في **مرحلة الإعداد**، وأنا كمقدم **أوافق على الإعداد كامل بكامل أسئلته**، ففي التصوير اللايف **كل ما أحتاجه تغيير ترتيب الأسئلة الموجودة**». → الميزة كلها ثلاث قطع: (أ) إعادة ترتيب موجود (ب) مصافحة المخرج يقترح ← المقدم يوافق (ج) زر البدء + المؤقّت على شاشة المخرج. **التوليد الحيّ والبدائل المُولَّدة مسبقاً وأي حقل يستلزم إعادة توليد التحضيرات — مقفولة بقرار خالد نفسه لا بتوصية منّا** (يبقى مطروحاً: حقل **مُشتق** بلا توليد وبلا migration إن احتاجه راشد). **ثابت إلزامي: الترتيب فقط — صفر تغيير نص، صفر حذف، صفر إضافة.**
- 2026-07-30 — **قرارات خالد النهائية + إعادة ترتيب الموجات (mariam)**: **الاتجاه ينقلب** (طاقة هادئة ← سؤال **يرفع** التوتر، تصحيح لا مطابقة) · **ثلاث درجات صريحة** هادئ/متوسط/حادّ — **والمخزَّن يظل ٠–٥** (تغييره يغيّر معنى صفوف `energy_change` التاريخية) وتُشتق بـ`energyBand` · زر البدء **يُضاف** للمخرج بنفس القفل · **مؤشّر المقدم يبقى بيده** أثناء البث (خالف توصية سارة) · المؤقّت = الزمن الصافي نفسه + سطر لزمن القسم · الاقتراح يسقط بعد **٩٠ث** والقيمة المعروضة تتحدّث فوراً دائماً → **تمييز إلزامي: «القيمة المعروضة» حيّة للجميع، و«المعتمَدة للترتيب» لا تتحرّك إلا بفعل المقدم** · وبكاتبَين في الغرفة: **فعل المقدم يفوز فوراً ويلغي الاقتراح المعلّق**. **الترتيب الجديد**: م١ عدّاد المخرج · م٢ زر البدء · **م٣ ضبط دالة الفرز (تقدّمت)** · م٤ الاقتراح/الموافقة · ثم موجة الإعداد. سبب التقديم الأقوى: **نصفا الدماغ يتناقضان اليوم** — `coachHint` تصحيحية (`energy.ts:115`) و`scoreQuestionByEnergy` مطابِقة (`energy.ts:67-70`)، فالهمسة تقول ادفع والترتيب ينزّل سؤالاً هادئاً؛ **هذا وحده يفسّر «ما فيه تأثير»**. **صفر أسئلة معلّقة على خالد** عدا واحد: شكل المؤشّر (٣ أزرار أو ٥ نقاط مُسمّاة) — قرار سارة داخل حكم «المخزَّن ٠–٥».
- 2026-07-30 — ⚠️ **تصحيح نطاق صريح (خالد ينقض حكمي جزئياً — وهو صاحب القرار)**: «الطاقة في الإعداد» (وسم كل سؤال بدرجة قبل التصوير) صارت **داخل النطاق، موجة منفصلة** بعد ما كانت مرفوضة. **رفضي السابق كان مشروطاً بـ«إعادة توليد التحضيرات القائمة» لا بالحقل نفسه** — والحقل هنا يُكتب للتحضيرات **الجديدة** فقط، داخل `prep_v2` JSONB: صفر migration · صفر backfill · صفر نص يتغيّر. **رأس معايير الموجة ومانع للشحن: تحضير قديم بلا الحقل يسلك تماماً كما اليوم — ترتيب متطابق سطراً بسطر عند كل درجة.** **«محرّر منحنى التوتر» → موجة ٣ لا هذه**: خالد ما طلبه، وهو سطح تأليف جديد يفتح سؤال «هل موافقة خالد على الإعداد تشمل المنحنى؟»، وقيمته لا تُقاس قبل تحضير موسوم حقيقي. (لا أنازع في قيمته — أنازع في توقيته.)
- 2026-07-30 — **حدود نطاق «صفحة المخرج + الطاقة» (mariam)**: داخل النطاق = زر بدء للمخرج **بنفس قفل التشك-ليست** · عدّاد حيّ على شاشة المخرج (إعادة استخدام `CompactClock`) · إسناد «مَن بدأ» **كعلامة بـ`author_id` لا كعمود** · تحويل تغيير المخرج للطاقة إلى **اقتراح+موافقة** · ضبط فرز `energy.ts` بعد قياس راشد. **خارج النطاق ويُرفض**: «سجل قرارات الطاقة» (مغطّى أصلاً بعلامة `energy_change`) · إيقاف/إنهاء/تغيير مرحلة للمخرج · أي migration أو نشر · عرض ساعتين معاً · صوت للتنبيه.
- 2026-07-30 — 🔴 **حكم التعارض مع بوابة التشك-ليست المنشورة اليوم**: زر بدء المخرج **تعديل لا إضافة** (يمسّ `change_phase:"host"` في `permissions.ts` + توجيه `recording-room-shell.tsx`). أربع قواعد: (١) زر المخرج **يخضع لنفس القفل** — أي زر لا يقرأ `isRecordingUnlocked` ينقض قرار خالد أمس. (٢) «تجاوز وابدأ» و«أكمل بنفسي» **للمقدم وحده** (صُمّما لغياب المخرج، فوجود زر عنده لا يبطلهما). (٣) الإسناد علامة بـ`author_id`، و`anchor_at` يظل مصدر الصفر الوحيد. (٤) **أول ضغطة تفوز** — الثانية تُرفض بهدوء بلا تيك ثانٍ.
- 2026-07-30 — **حقيقتان تغيّران شكل الطلب** (قرأتهما بالكود): (١) المخرج **اليوم** يغيّر الطاقة مباشرة وبصمت (`DirectorEnergyControl` + `change_energy:"director"`) — فطلب خالد **تعديل مسار قائم** وليس ميزة جديدة، وهو **يقلّل** ما هو منشور. (٢) `must_ask` **يسبق الطاقة** في مقارن `energy.ts` — فأي قسم كل أسئلته `must_ask` أثر الطاقة فيه **صفر رياضياً**؛ إن أثبته قياس راشد فالحل **ضبط معامل لا بناء**، ويُقدَّم لأنه أرخص وأسرع إحساساً.
- 2026-07-30 — **معيار إغلاق شكوى «الطاقة بلا تأثير»** — المقياس هو **حركة الترتيب** لا غير: خط الأساس X الحالي يُقاس ويُكتب أولاً (راشد)، ثم النقل ٥→١ **يبدّل السؤال المعروض التالي في ≥X٪ من الأقسام** (٣ أسئلة فأكثر) — الهدف المقترح ٧٠٪ ويُثبَّت بعد وصول خط الأساس. **إن طلع الأساس صفراً أو قريباً منه فهذا تفسير الشكوى نفسها.** + **حكم خالد بعد أول تصوير** — الشكوى كانت إحساساً فلا تُغلق بغير إحساس مؤكَّد برقم. ومعه **ثابت يُفحص مع كل معيار**: مجموعة معرّفات الأسئلة ونصوصها متطابقة حرفياً قبل وبعد، والفرق الوحيد المسموح هو الترتيب.
- 2026-07-30 — **إعادة التصوير (قرار خالد نهائي)**: نفس الحلقة ترجع `ready_to_record` كانتقال `retake` — خالد **وافق صراحةً على فتح رجوع للخلف** في عقد `lib/eir/transitions.ts` المُجمّد بقصد. + جدول `room_takes` (`anchor_at` تُكتب مرة واحدة · `camera_offset_ms`) + `room_session_markers.take_number` + إعادة تسمية `recording_ms` → `net_recording_ms`.
- 2026-07-30 — **التصدير**: CSV + EDL على **24 fps**. خالد اختار الاحتفاظ بـ EDL بعد ما عُرض عليه الدليل إن Resolve يُسقط العربي منها **بصمت**؛ الحل المتّفق عليه: EDL تحمل الموضع والنوع الإنجليزي واللون، والـ CSV جانبها تحمل الملاحظات العربية = **صفر فقدان**.
- 2026-07-30 — **تشك-ليست المخرج**: ١٧ بند / ٦ مجموعات، تتفتّح **بالتدريج بالمجموعة** (مو بند-بند)، مع «غير منطبق بسبب». زر «ابدأ التسجيل» **يُقفل فعلياً** — خالد رجّح القفل على توصية سارة الأخف، بس مع **مخرجَي أمان وافق عليهما**: «أكمل التشك-ليست بنفسي» + «تجاوز وابدأ» موثّق بسبب. «Black shade» = فلتر **Black Mist** على العدسة → صار عنصراً لكل كاميرا مو بنداً عاماً.
- 2026-07-30 — **ثلاث مشاكل قائمة أكبر من الملاحظات نفسها**: (١) **انزياح الـ pause** — الكاميرا تستمر تسجّل وقت الإيقاف المؤقت بينما `recording_ms` يستبعده، فكل علامة بعد أول استراحة منزاحة والانزياح **يتراكم**؛ لا فقدان بيانات (خالد أكّد إن كل تصوير سابق كان تجارب). (٢) **اختلاط التيكات** — ما كان فيه أي عمود يفصل تيكات إعادة التصوير. (٣) **المخرج ما يشوف شاشة «قبل البدء» أصلاً** — `recording-room-shell.tsx` يوجّه `host` فقط لكوكبيت المقدم.
- 2026-07-30 — ⚠️ **شرط تشغيلي على خالد**: حساب المخرج لازم يكون **`ADMIN` بالضبط** — لا EDITOR ولا OWNER. وخالد نفسه OWNER، فما يقدر يكون المخرج في نفس الغرفة.
- 2026-07-30 — **تصحيح حالة قاعدة البيانات المحلية (يُلغي drift ٢٥ يوليو)**: تحقيق يوسف أثبت إن الـ drift المحلي انصلح **نفس الليلة** في commit `ba2d918`؛ الـ ledger المحلي الآن **١٩/١٩ نظيف** و`db:generate` آمن (متحقّق بـ dry-run). شرطان قبل أي migration: **backup بـ `pg_dump` نسخة @16** + **مراجعة الـ SQL المولّد بالعين**. `db:push` و`db:adopt` **ممنوعان**.
- 2026-07-30 — 🔴 **مفتوح ويحتاج خالد — بطاقات الإسناد لا تصله**: الميزة **مبنية بالكامل** (٧ أنواع، ومنها `correction` ببنية «إن قال الضيف كذا ← الصحيح كذا»، بنصّ جاهز للقراءة بصوت عالٍ ومصادر حقيقية). سبب الغياب: **سقف إنفاق Gemini الشهري مستنفَد** ⇒ كل نداء إسناد 429 ⇒ الخط **يولّد ٤٨ مرشّحاً ويُبقي صفراً**. مؤكَّد بنداء مباشر مرتين (ليست مشكلة كودنا ولا المفتاح). خالد رفع سقفاً ولا يزال يرفض ⇒ الأرجح أنه رفعه على **مشروع غير مشروع المفتاح**. وسلسلة الصمت بعدها: التمريرة غير حاسمة ⇒ التحضير يعلن `ok=true` · شاشة الاعتماد **تختفي كلياً** حين تكون فارغة (`return null`) · استوديو التحضير لا يذكرها إطلاقاً · وشاشة البث تصمت. **+ سقفنا نحن `MAX_GROUNDING_CANDIDATES=30`** يرمي ~١٨ مرشّحاً حتى لو اشتغل Gemini.
- 2026-07-30 — 🟠 **مفتوح — عرض بطاقات الإسناد يخذلها حتى لو وصلت** (سارة، مقيس): نص التصحيح **١٣px** أصغر من نص المتابعة **وهو ما يُقرأ بصوت عالٍ** لمناقضة ضيف على الهواء · البطاقات **مطوية دائماً** رغم تعليق يقول إن الـhero يفتحها · `timing` (قبل/أثناء/بعد) **معطّل تماماً** · المصدر ودرجة الثقة تحت نقرتين في لحظة قرار المناقضة · و**الكهرماني يحمل سبعة معانٍ** فلا يحمل إلحاحاً. القاعدة المقترحة: ما يُقرأ بصوت عالٍ ≥١٧px.
- 2026-07-30 — 📌 **أول تحضير حقيقي في القاعدة**: `2fdb6a84-b8e5-4ae3-9630-c12d820fa5b4` (الأستاذ علي دريساوي، ٢٨ سؤالاً، ٦ أقسام). ⚠️ **الرقم مُصحَّح 2026-07-31: التكلفة الحقيقية `$1.2311` على ١٦ صفاً، لا `$0.8105` على ١٠** — الفرق أن **الإسناد اشتغل مرتين**، ووحده `$0.8098` = **٦٦٪ من كلفة التحضير مقابل صفر بطاقة وصلت**. أي قياس قديم بنى على `$0.8105` يُعاد. **لا يُحذف ولا يُعاد توليده** — هو العيّنة المرجعية لكل قياس على توليد الأسئلة. قبله كانت كل التحضيرات fixtures و`ai_runs` فيها **صفر** صف للتحضير، وهذا عطّل وكيلين عن القياس. ونموذج السعر صار **مؤكَّداً على الإنتاج** لا مستخرجاً بانحدار (توقّع `$0.04065` مقابل `$0.0406`).
- 2026-07-30 — 🟡 **مفتوح صغير**: تصدير الـCSV لا يفرّق بين «حرّك المخرج القرص» و«وافق المقدم» — كلاهما «المستوى ٥»، لأن العمود يُبنى من `note` وحده و`label` لا يصل. الأثر مسجّل في القاعدة بشكل صحيح وقابل للاستعلام؛ الناقص العرض فقط.
- 2026-07-30 — ✅ **نشرة ثالثة بأمر خالد**: مؤشّر الطاقة (`e78d280`، **صفر migration**، ledger يظل ٢٣). شكوى خالد «ما يعطي تأثير» كان لها **ثلاثة أسباب متراكبة**: (١) `scoreQuestionByEnergy` بلا فرع `medium` والافتراضي ٣ ⇒ كل سؤال صفر ⇒ الترتيب no-op؛ (٢) `heroId` يثبّت السؤال بلا مصفّر ⇒ أول ضغطة من المقدم تقتل أثر المؤشّر للأبد؛ (٣) الاتجاه **مطابِق** بينما `coachHint` **تصحيحية** ⇒ الشاشة تناقض نفسها. + المخرج كان يعيد ترتيب قائمة المقدم **بصمت أثناء إجابة** ⇒ صارت اقتراحاً ينتظر الموافقة (القيمة فورية، الترتيب لا). + **اكتشاف**: `startTimer/pause/resume/end/setCurrentSection` **ما تبثّ إطلاقاً** ⇒ شاشة المخرج كانت متأخرة دائماً ومحد يدري.
- 2026-07-30 — **قرار خالد التحريري يُلغي الطاقة الصريحة**: «لا أحتاج في كل قسم خيار حاد». راشد قاس على أول تحضير حقيقي (`2fdb6a84…`، ٢٨ سؤالاً — التكلفة مُصحَّحة لاحقاً إلى `$1.2311`): **٤ أقسام من ٦ بلا أي سؤال حادّ**، وأثبت أن وسماً صريحاً **بدقّة ١٠٠٪** يساوي الدالة الحالية بالضبط — لا إشارة تُظهر شدّة غير موجودة في البنك. ⇒ أُلغي الحقل الصريح **وتعديل برومبت Pass 3**، وحلّ محلهما **سطر صدق** يُشتق من البيانات: «أسئلة هذا القسم متقاربة الشدّة». الصمت في تلك الأقسام كان **كل** الشكوى.
- 2026-07-30 — 🔴 **درسان من جولة الطاقة**: (١) **قرار تصميم صحيح أنتج الخلل** — فصل «المعروضة» عن «المعتمَدة» كان لازماً، لكن أحداً لم يسأل «ماذا لو انفصلا ثم لمس المقدم القرص؟» فحارس النقر قارن بالخطأ منهما و**مات الزر في قمرة قيادة حيّة**. ٢٠٥٢ اختباراً ما مسكته لأنها كلها تختبر المنطق، والخلل في **الجسر بين المنطق واليد** — ما كشفه إلا إصبع حقيقي. (٢) **قراءة-ثم-كتابة ليست ذرّية**: ضغطتان متزامنتان «تفوزان معاً» ٤ من ٥ حتى انتقل الحسم للقاعدة (`UPDATE … WHERE status <> 'live' … RETURNING`).
- 2026-07-30 — 🔧 **حقيقة أدوات عطّلت التحقق طول اليوم**: إحداثيات النقر في هذي البيئة بمقياس **×٣.٠٨ أفقياً و×٣.٠٦ عمودياً** (نقرة `[100,100]` تصل CSS `(308,306)`)، و`read_page` يرجّع شجرة فارغة و`innerText=0` فتبدو الصفحة ميتة وهي سليمة. **عايِر بمستمع DOM أولاً** ثم الواجهة كلها قابلة للنقر. وكيلان استنتجا خطأً أن «النقر لا يعمل».
- 2026-07-30 — ✅ **نشرة ثانية بأمر خالد**: اسم العضو و«الصفحة» (`28c8b5a`، ledger **22 → 23**). `admin_users.display_name` + `job_title` (nullable)، كتالوج سبع صفحات في الكود، و`resolveRoomRole()` يرجّح الصفحة على الاشتقاق من مستوى الصلاحية. **يُلغي قيد «حساب المخرج لازم ADMIN بالضبط»** — خالد يصير مالكاً ومقدماً في نفس الوقت. مُتحقَّق حياً: نفس حساب ADMIN، `job_title='host'` → كوكبيت المضيف، و`null` → شاشة المخرج. **باقٍ على خالد**: تعبئة الأسماء والصفحات الستة من `/admin/team` — قبلها الـCSV يقول «عضو الفريق».
- 2026-07-30 — 🔴 **درسان من نشرة الصفحات، يُقرآن قبل أي ميزة صلاحيات**: (١) `requireRoomRole` في `lib/collaboration/permissions.ts` **حارس تفويض حقيقي** كان يقرأ `room_participants.role`، وهذا العمود صار إسقاطاً لـ`job_title` — فحقل وصفي منح `delete_marker` و`end_room` وتغيير المرحلة على خمس نقاط. صار يشتق من `admin_users.role`، و`room_participants.role` للعرض فقط. (٢) **الاختبار اللي «يثبت» أن الصفحة ما تمنح صلاحية كان فراغاً** — يمرّن `requireActionRole` وهي لا تقرأ `job_title` فما كان يمكن يفشل. القاعدة: أي تأكيد «X لا يمنح صلاحية» لازم يمرّن الحارس اللي **يستهلك** X، لا حارساً يتجاهله. الاختبار الجديد مُثبَت بإرجاع الإصلاح (٦ من ١١ فشلوا).
- 2026-07-30 — 🔴 **صنف خلل عدّى كل البوابات مرتين في نفس اليوم**: مقارنة نوع SQL خاطئة (`uuid = text`) جعلت التصدير يرجع **500**، وعدّت من `tsc` ومن `next build` ومن ٢٠١٨ اختبار **لأن ولا واحد منهم ينفّذ SQL**. لم يُكتشف إلا بتنزيل HTTP حقيقي. الـsmoke صار ينفّذ استعلام التصدير الفعلي. **أي استعلام يُعدَّل لازم يُمرَّن على Postgres حقيقي.**
- 2026-07-30 — ✅ **نُشر على الإنتاج بأمر خالد الصريح («انشر»)**. Migrations **0019–0021** مطبّقة، الـ ledger **19 → 22/22 مطابق للكود**، و`room_takes` (٧ أعمدة) و`recording_checklist_items` (٩ أعمدة) وإعادة التسمية `recording_ms → net_recording_ms` كلها حيّة. `post-schema.sql` أُعيد تطبيقه. العمليتان `online` وكل المسارات العامة ٢٠٠. نسخة احتياطية للإنتاج قبل أي شي: `/root/db-backups/khat-prod-20260730-073504.dump`.
- 2026-07-30 — 🔧 **عقبتا نشر مثبَّتتان — تُقرأ قبل أي نشر قادم فيه migration**: (١) `npm run build` **ينفد من ذاكرة Node عند خطوة TypeScript** على الدروبلت (~٩٨٠MB، الحد الافتراضي) رغم وجود ٤GB swap — الـswap ما ينفع، السقف داخلي. الحل: `export NODE_OPTIONS=--max-old-space-size=3584`. (٢) `drizzle-kit migrate` **يتوقف للأبد على `[⣷] applying migrations...` عبر SSH غير التفاعلي** — بلا خطأ، بلا صف ledger، exit 1، والقاعدة ما تُلمس. ليست مشكلة SSL ولا env (الإعداد يستبعد `sslmode` و`psql` يتصل من نفس السيرفر)، و`setsid nohup` ما ينفع. الحل: **`scripts/migrate-node.ts`** (مهاجر drizzle-orm الداخلي — يكتب الـ ledger بنفسه فما فيه أي كتابة يدوية، وهي اللي عطبت التاريخ سابقاً). يُشغَّل بـ `KHAT_DB_TARGET=production npx tsx scripts/migrate-node.ts`. **ممنوع إدخال صفوف ledger باليد.**
- 2026-07-30 — **ترتيب النشر إلزامي بسبب إعادة التسمية**: رفع → بناء (بطيء) → migration → إعادة تشغيل **فوراً**. نافذة رؤية الكود القديم لعمود مُعاد التسمية تصير ثوانٍ. وأخطاء `Failed to find Server Action "…"` بعد إعادة التشغيل **طبيعية وعابرة** (تابات قديمة تحمل معرّفات قديمة)، ليست خللاً.
- 2026-07-30 — ✅ **أُغلق**: خالد سوّى فحص إغلاق تاب المخرج بنفسه على الإنتاج وأكّد أنه يشتغل، وعبّى الأسماء والصفحات الستة من `/admin/team` — فالـCSV صار يحمل أسماء عربية حقيقية بدل «عضو الفريق». **ما بقي أي بند معلّق من ملاحظات صفحة التسجيل.** (الملاحظة الأصلية للتاريخ:)
- 2026-07-30 — ~~🔴 **الفحص الوحيد المتبقي**~~: مسار الحضور عند **إغلاق تاب المخرج** لم يُجرَّب في متصفح حقيقي أبداً (نورة انقطعت ٥ مرات بخطأ خدمة 529). الخطأ الأصلي: `navigator.sendBeacon` **دائماً POST**، وPOST على مسار الانضمام = انضمام، فالمخرج اللي يقفل تابه يعلّم نفسه حاضراً ويحبس المقدم للأبد. الإصلاح (DELETE بـ`keepalive` + نبضة أولى فورية + تثبيت اللاتش بعد نجاح الـawait) مؤكَّد على مستوى الكود فقط. **الفحص: افتح بحساب المخرج، سكّر التاب، تأكد أن شاشة المقدم تنتقل لـ«ما فيه مخرج متصل».**
- 2026-07-30 — **شرط تشغيلي على خالد**: حساب المخرج لازم `ADMIN` **بالضبط** (`OWNER→host`, `ADMIN→director`, `EDITOR→editor`) — EDITOR ما يشوف القائمة، وOWNER يصير مقدّماً. وخالد OWNER فما يقدر يكون المخرج في نفس الغرفة.
- 2026-07-30 — ✅ **لغز «٦٦ → ٤١ حلقة» انحلّ**: خالد سوّاه بنفسه في جلسة منفصلة **بقصد**، ليجعل local مطابقاً لـlive بالضبط. ليس حادث فقدان بيانات ولا خلل أداة. **فأي فرق مستقبلي بين local وprod صار إشارة حقيقية لا ضجّة.**
- 2026-07-29 — **حدود نطاق «التصنيفات في الموقع العام» (mariam)**: داخل النطاق = `components/episodes/episode-poster-card.tsx` · `app/episodes/page.tsx` · `app/episodes/[slug]/page.tsx` · مسار جديد `app/categories/**` · تمرير اسم/سلاق التصنيف في مسار القوائم بـ`lib/queries/episodes.ts` (بلا N+1) · `app/sitemap.ts` إذا وافق خالد. **خارج النطاق ويُرفض**: تعديل نظام التصنيفات نفسه أو جدول `episode_categories` · دمجه مع `/topics` · تصنيفات متعددة للحلقة · إعادة تصميم `/episodes` · وصف/صورة للتصنيف (يحتاج migration) · أي تغيير في لوحة التحكم · أي نشر أو migration.
- 2026-07-29 — **حقائق مثبَّتة قبل البدء**: `episode_categories` فيه ٣ صفوف بسلاقات **عربية** (`سالفة` ١٦ · `مقاطع-خط` ٦ · `الموسم-الاول` ١٩)، ٤١/٤١ حلقة مصنّفة، صفر يتيم · `getEpisodes({category})` + `filterByCategory()` **موجودان وشغّالان** فالفلتر ما يحتاج طبقة بيانات جديدة · مسار القوائم يعبّي `category_id` فقط ولا يعبّي كائن `category` (الاسم/السلاق) — يُعبَّأ فقط في مسار التفصيل · `EpisodePosterCard` **مشترك** بين الرئيسية و`/episodes` و`/guests/[slug]` و`/topics/[slug]` والتوصيات، فأي شارة فيه تظهر في الخمسة · سابقة `decodeURIComponent(slug)` + `notFound()` داخل `generateMetadata` موجودة في `app/guests/[slug]/page.tsx` (تمنع soft-404).
- 2026-07-27 — 🚨 **حاجز نشر مثبَّت بالدليل**: 5 من 7 اقتباسات نُسبت للضيف «فيصل المحيني» **غير موجودة في الحلقة** (تطابق 25-43%، تحقق عمر المستقل). و`push-to-episode.ts:189` يكتب `status:"published"` ثابتاً، و`speaker` **لا يصل الموقع العام** فكل اقتباس يُنسب للضيف بالاسم والصورة مع زر يولّد صورة مشاركة. **ممنوع نشر أي اقتباس مولّد من الاستوديو قبل مطابقته بشرياً على النص.** الإصلاح الفوري سطر واحد (`"published"` → `"draft"` — الـRPC والقراءة العامة يدعمان `draft` أصلاً).
- 2026-07-27 — **تصحيحات على تقرير الفحص (انتفت أو صُحّحت)**: انحراف التوقيتات +52/+69 → **الأقصى الحقيقي +38.3د والوسيط +20.4د** · «الوسيط 68 حرفاً للعناوين» → **الوسيط 46 و6/6 تحت حد الجوال، العناوين سليمة** · «محرّر المقاطع يرمي المقاطع» → **لا أحد يرفضها، تمر صامتة** · **ضياع SSE ما ظهر (30/30)** حقيقي بالكود ولا ينطلق على localhost · **`maxDuration=300` ما قطع** (الخط 522ث؛ `next dev`/PM2 لا ينفّذانه) · «الاقتباسات التاريخية 8/8» → **0/8 مفبركة لكن 6 حرفية و2 إعادة صياغة** · «التوقيتات مجانية» → **على مسار الكابشنز فقط؛ Whisper يحتاج +$1.2954**.
- 2026-07-27 — **التكلفة المقيسة تُلغي التقدير**: **$2.5996 لحلقة 216 دقيقة · 48 نداء** (منها الخط الثماني $1.3042 وWhisper $1.2954 و13 نداء ضغط $0.3851). التقدير الأول (~$2.00 لحلقة ساعتين) كان **أعلى بـ39% للدقيقة وأقل بالنصف في عدد النداءات**. الحلقة بعد الخطة ≈ **$1.07 (−59%)**. نسبة العربي المقيسة **3.08 حرف/توكن** — لا تُستخدم قاعدة «4 أحرف/توكن».
- 2026-07-27 — **ق-1 و ق-5 و ق-6 تغيّروا بعد التشغيل الحي**: ق-1 (النص الكامل) من **+$0.58 كلفة** إلى **−$0.23 توفير** مع `prompt caching` (توصية: البديل الآمن `B′` أولاً، −$0.12 بلا إعادة ترتيب برومبتات) · **ق-5 انقلب**: Whisper ما اشترى دقة أسماء (الكابشنز أفضل في 11 اسماً مقابل 3) ورمى التوقيتات المجانية — البوابة تُرفع **لتفضّل الكابشنز** وWhisper احتياط فقط · **ق-6 ما عاد قراراً، صار حاجز نشر ينفَّذ فوراً**. ق-2/3/4/7/8 بلا تغيير جوهري.
- 2026-07-27 — **قرار جديد ق-9 بانتظار خالد**: قياس أثر `reasoningEffort` على الفصول والمقاطع = 4 نداءات ≈ **$0.10 صرف مدفوع**. راشد ما يشغّله بلا موافقة.
- 2026-07-27 — **ترتيب التنفيذ المعتمد**: موجة 1 (بوابة الاقتباسات → هوية الربط → الحذف المدمّر → عنوان الحلقة) · موجة 2 صفر تكلفة وتوفير صافٍ (الكابشنز افتراضاً → التوقيتات من الـVTT · `max_tokens` · حارس الكاش **متلازم مع** زر إعادة التوليد · بتر النص والجزء السادس) · موجة 3 النص الكامل (**يجب أن تسبق أي حكم على الجودة**) · موجة 4 صدق الحالة · موجة 5 النمو · موجة 6 الهيكلة والقياس.
- 2026-07-27 — ⚠️ **النسخة الاحتياطية ليست ضمانة**: `lib/episodes/versions.ts:33-36` يبتلع فشل الـFK ويرجّع stub، وبما أن `episodes.id` = معرّف يوتيوب وكثير من الحلقات بلا صف في الجدول، `saveVersion` **ينسكب بصمت في الحالات نفسها التي نريد حمايتها**. لا يُعتمد عليها قبل إصلاحها (ص-27).
- 2026-07-27 — **قيد مهمة الاستوديو**: ممنوع تعديل كود المصدر قبل تسليم التقرير كامل لخالد (أمر صريح منه). local فقط، production مجمّد لهذي المهمة. التوصيات تنكتب ولا تُنفَّذ. **الحالة: التقرير سُلِّم؛ القيد باقٍ لين خالد يقول «نفّذ».**
- 2026-07-27 — **8 قرارات موقوفة على خالد** قبل أي تنفيذ (تفاصيلها في التقرير §6): ق-1 تمرير النص الكامل (+$0.58/حلقة، بديل أرخص +$0.20) · ق-2 اللهجة الكويتية للخطّاف · ق-3 المقاطع 10-20 موزّعة → 8 مرتّبة · ق-4 طول المقطع حتى 180 ثانية · ق-5 رفع بوابة جودة النص (+$0.72 على الحلقات الرديئة) · ق-6 الاقتباسات مسودة بدل نشر مباشر · ق-7 مصير المولّدات اللي تطلب المستحيل · ق-8 الهيكلة 12→7.
- 2026-07-27 — **موجة 1 لا تحتاج أي قرار** (الحذف المدمّر، زر إعادة التوليد، السقوف المخنوقة، كذب الحالة، عنوان الحلقة، الحاكم، البث، جسر المقاطع). يوسف يعتبر بندين منها **مانعين للنشر**. ⚠️ لا يُختبر إصلاح الحذف على قاعدة فيها شغل حقيقي بلا نسخة احتياطية.
- 2026-07-27 — **تحذيران تشغيليان لخالد من اليوم** (بلا كود): لا يُحذف أي studio session لحلقة منشورة · زر «إعادة التوليد» لا يعمل، فالحل الوحيد جلسة جديدة.
- 2026-07-27 — **حد المصداقية**: خط إنتاج الاستوديو ما اشتغل كامل ولا مرة محلياً. الحكم على الجودة مبني على **مخرج واحد (الفصول) من حلقة 8:24**. أي حكم أوسع يحتاج تشغيل حلقة صوتية كاملة أولاً.
- 2026-07-27 — **حدود نطاق فحص الاستوديو (mariam)**: داخل النطاق = `app/admin/studio/**` · `lib/studio/**` · مولّدات الاستوديو في `lib/ai/` · مسارات إدخال النص (yt-dlp captions / youtube-audio / Whisper) · حتى حدود `push-to-episode.ts`. **خارج النطاق ويُرفض**: discovery / season planning / الموقع العام / `/admin/ops` / market intelligence / هوية الضيف / أي migration أو تنظيف بيانات أو نشر.
- 2026-07-27 — **حكم العربية بشري لا آلي**: بناءً على بنشمارك 2026-07-26 (حكم الموديل طابق خالد 35% مقابل 34% صدفة)، أي حكم على جودة العربية بهذا الفحص يجي من خالد أو مرزوق — ممنوع الاستشهاد بحكم موديل كدليل جودة لغوية.
- 2026-07-25 — **تفويض خالد**: الفريق يحسم القرارات المهنية بنفسه بلا أسئلة ارتجاعية، والسقف = أفضل تجربة لا الحد الأدنى. (لا يشمل: الحذف/النشر/الإنتاج — تظل بموافقة صريحة.)
- 2026-07-18 — أي حذف / تصفير / ترحيل / كتابة فوق بيانات — محلي أو إنتاج — يحتاج موافقة صريحة من خالد.
- 2026-07-18 — التواصل مع خالد بالعربي الكويتي الواضح؛ الكود والمصطلحات التقنية تظل بالإنجليزي.
- 2026-07-18 — راشد يملك بنود طبقة الـ AI (تجاوزات getClient خارج الراوتر، صدق تليمتري `ai_runs`، قرارات الموديلات عبر benchmarks، مراجعة البرومبتات) — والتنفيذ يظل عند فهد وحده.
- 2026-07-21 — كل محتوى «التفكير الأصيل» و«إشارات السوق» عربي بالكامل؛ عناوين المصادر الخام الخارجية (YouTube/Apple) تبقى كما هي.
- 2026-07-21 — **تنبيه بنيوي قائم**: `persistSignal` (`lib/market-intelligence/ingestion.ts`) يسوي `ON CONFLICT DO UPDATE` يطمس `view_signal` → ما فيه سلسلة زمنية، وقياس الزخم مستحيل بلا snapshots. نفس القيد يمنع أي «سهم اتجاه» بأي صفحة.
- 2026-07-25 — **رُفض بمبرّر (منع تضخّم `/admin/ops`)**: ملخّص AI للرئيسية · أزرار قبول/رفض من صفحة قراءة · أسهم اتجاه. **قُبل**: تحديث كل 60ث يتوقف لما التبويب مخفي · نزول بطاقات النبض تحت الـ launchpad.
- 2026-07-25 — **مدى `teaser_questions` مقفل**: شريحة بالوارد (`status='pending'`) + صفحة `/admin/teaser-questions` بثلاثة إجراءات عبر `requireActionRole('EDITOR')` + (تابع اختياري) نموذج عام. **صفر migration وصفر عمود جديد**؛ أي حقل تدقيق (`reviewed_by`/`reviewed_at`) خارج المدى. القرّاء يرجّعون أعمدة عامة فقط — `ip_hash`/`user_agent` ما تغادر السيرفر.
- 2026-07-25 — **تصحيح واقعي مثبّت**: ما فيه نموذج عام لأسئلة الجمهور إطلاقًا؛ `POST /api/teaser/[id]/questions` ما يناديه أي كود. `teasers` = 0 صف · `teaser_questions` = 0 صف · وكل قنوات الوارد البشرية = 0 صف.
- 2026-07-19 — **قسم دعوة الضيف — قرارات خالد** (تنتظر «نفّذ»): يجهّز الرسالة ويرسلها بنفسه (صفر إرسال مؤتمت) · وافق/رفض يدوي عبر `status.ts` · تمديد `guest_candidates` لا قسم مكرّر · التلفون/الإيميل ينتقلان لجدول `guests` عند الترقية (إداري غير عام) → يحتاج عمودين + migration.
- 2026-07-19 — **بندان مُتتبَّعان لا يحجبان**: ترحيل 137 موضع server actions لـ `requireActionRole('EDITOR')` · `npm run lint` يفشل على 8 أخطاء `no-explicit-any` قديمة بملفّين خارج شغل فهد.
- 2026-07-19 — **مقترح مقبول**: صفحات 404 الأدمنية ترجع HTTP 200 (قيد streaming بالـ App Router) — وظيفيًا سليمة، نقبل الـ200.
